{
  "id": "31",
  "subject": "❓ [parked] requirements.yaml: reverse R26/R33/R34/R59/R84/R88, trim R77/R81/R82, rename R87/R91's test titles, for Pass 4 (ask-guard.sh + stop-autopilot.sh retirement) — same blocker as #30, contract-guard denied this edit while writing it; because: ask-guard.sh (auto-deny+auto-park AskUserQuestion) and stop-autopilot.sh (forced Stop-hook continuation, ship-mode auto-commit, stall/run-bound caps, sweep terminator, burn-down handoff) are both deleted; hooks.json now has only UserPromptSubmit left; STEERING's autopilot block rewritten to state everything is advisory-only and explicitly note there is no more auto-park and no more automatic stall/run cap; ship-mode's commit-to-branch logic survives as standalone bin/ship-checkpoint.sh (same guarantees, manually invoked); tq stopfields (already-existing, previously only read by stop-autopilot.sh) now carries the sole surviving test coverage for the 'first startable task' and sweep-eligibility selection logic, driven directly instead of through the deleted hook; proposed text — R26 'autopilot is a persisted, advisory preference, not enforced' (full reversal, this is THE biggest fidelity loss of the whole R100 redesign — R28's own 'the rule exists to end the re-litigation' guarantee is what's being removed); R33 'taste choices are STATED to be parked, not auto-decided — advisory, no guard left' (soften to advisory, verified_by → the new STEERING-prose-existence test); R34 'ship-mode commits to a work branch, ON REQUEST' (auto→manual, verified_by → the 3 renamed ship-checkpoint.sh tests + the 2 unaffected statusline ones); R59 unchanged core text, verified_by swaps the ask-guard-flip test for a toggle-persistence-only one + the new STEERING-prose test; R77 trim ONE entry (autopilot sweep TERMINATES — the swept-counter cap has no surviving code, sweep's eligibility logic itself is fine and re-tested via tq stopfields directly); R81 trim FIVE entries (2 stall-cap + 3 run-bound tests — no surviving code, the tq-prune and hook-budget-self-test entries are unaffected and stay); R82 trim ONE entry (drained-queue-hands-off-to-burn-down wiring — no surviving code, burn-down.sh/candidates.sh/burndown-branch.sh keep their own 13 direct tests unaffected); R84 full reversal 'a blocked question must be parked BY THE MODEL — nothing parks it automatically anymore' (verified_by drops the 2 ask-guard tests, keeps the tq-concurrency test and prompt-continue test which never depended on ask-guard, adds the STEERING-prose test); R87 NO reversal, just rename its one verified_by title (Stop:... → tq stopfields:..., same underlying tq logic, now driven directly); R88 the requirement ITSELF is now moot — nothing left automatically touches the autopilot flag on a dry queue at all (only explicit autopilot.sh/resume.sh do), propose changing to judgment: tagged, verified_by: []; R91 NO reversal, rename its one verified_by title (the DRAIN and the burn loop → tq stopfields and the burn loop); rec: apply as staged, same as #30 — this is Pass 4 of the already-approved R100 plan",
  "status": "completed",
  "done_when": "owner reviews the staged R26/R33/R34/R59/R77/R81/R82/R84/R87/R88/R91 text (same review session as #30 makes sense — same blocker, same plan) and either applies it or redirects",
  "context": "",
  "blocks": [],
  "blockedBy": []
}
