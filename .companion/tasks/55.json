{
  "id": "55",
  "subject": "C1: bin/seen-gate.sh — rubber-stamp guard for --seen, mirroring da-gate.sh note: trim, ASCII-fold for the DENYLIST COMPARE ONLY (da-gate's recorded R1 bug: tr -cd is byte-oriented, folded a substantive Japanese finding to 0 chars and refused it), min length on the untrimmed-of-unicode string, denylist of self-referential proxy claims (tests pass / compiles / typechecks / committed / done / works / it works). NAMED LIMIT in the header: this is a generic-English shape check, not an ecosystem allowlist (R9), and it is fabricable by design — it raises the floor, it does not lock the door.",
  "status": "completed",
  "done_when": "seen-gate.sh accept exits 0 on 'opened the review screen in Expo Go on the device and the media loaded', exits 2 on 'tests pass' and on a 3-char string, and does NOT reject a substantive non-ASCII finding",
  "context": "plugins/companion/bin/da-gate.sh (the precedent to mirror, incl. its non-ASCII bug comment), plugins/companion/bin/seen-gate.sh (new), docs/requirements.yaml R9",
  "blocks": [],
  "blockedBy": [],
  "seen": "ran seen-gate.sh check directly against 8 inputs in a shell: accepted a real observation and a Japanese one, refused 'tests pass'/'it compiles'/'done'/'ran it'/empty/uppercase/flag/newline"
}
