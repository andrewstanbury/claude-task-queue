{
  "id": "99",
  "subject": "restore the Stop-hook burn-down hand-off (#97 option A): on a dry queue with autopilot AND burndown armed, stop-autopilot.sh calls should_burn and, on BURN, continues the turn pointing at rank-1 — and the STEERING/comment prose is rewritten in the SAME change so ownership is not duplicated and no doc goes false",
  "status": "completed",
  "done_when": "a dry-queue stop with both modes armed and should_burn=BURN continues the turn naming rank-1; holds/ends the turn otherwise; stop-autopilot.sh comment + STEERING prose updated together; bounded per R81 and measured by hook-budget; tests + mutation; check.sh green",
  "context": "plugins/companion/bin/stop-autopilot.sh:14-26 (the deliberate omission being reversed), plugins/companion/bin/burn-down.sh should-burn, plugins/companion/STEERING.md, dev/hook-budget.sh",
  "blocks": [],
  "blockedBy": [],
  "seen": "drove the real Stop hook through its JSON stdin in a scratch repo across all four states: burndown off → turn ends; armed with no snapshot → holds and ends; armed with a BURN-worthy two-sample snapshot → refuses the stop and names candidates + burndown-branch; and the behavioural test caught a real placement bug where an EMPTY store bailed before the hand-off, now called from both dry-queue exits. Hook budget measures the new path at 23ms."
}
