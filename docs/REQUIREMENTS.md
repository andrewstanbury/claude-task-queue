# REQUIREMENTS — the locked / challengeable ledger

The **single source of truth** for this project's durable requirements. CLAUDE.md
invariants and [docs/ROADMAP.md](./ROADMAP.md) reference entries here **by ID** instead
of restating them — this file is where a requirement's *status* lives. *(The 2026-07-11
ground-up rebuild collapsed the four plugins into one `companion`; entries touched by it
are annotated below — see **R24**.)*

*Organized **chronologically by R-ID** (this ledger's native axis — it's decision history).
It sits at the centre of the shared spine: the contract (`docs/flows/`, `docs/INVARIANTS.md`)
references **this** file **by ID**, not the reverse — so there are no per-row UX
links here (that direction would duplicate the pillars, against R2 one-canonical-home).*

**Status vocabulary**

- 🔒 **locked** — *changing* it needs explicit owner sign-off; but *challenging* it is
  **invited** (R52·3) — the recommendation loop should actively surface challenges to locked
  entries rather than treat them as settled. Silence-by-deference is itself a miss.
- 🔓 **open** — up for challenge; the loop may propose changing these on its own signal.
- ⚰️ **retired** — no longer in force; kept for history with a pointer to what replaced it.

**The rule (R5):** every recommendation and every task-conversion **cites the R-IDs it
touches and names any it would change** — a requirement is never retired silently, in
either direction.


**Density standard (R66/R3, 2026-07-22):** entries are Claude-consumed — dense spec, not
narrative: constraint · status · provenance+date · the load-bearing why · amends/composes
links. Blow-by-blow history lives in git, not in rows. R1–R31 predate the standard (left
as-is this pass — compact opportunistically when touched); R32+ conform.

| ID | Status | Requirement | Rationale · Retires · Supersedes |
|---|---|---|---|
| **R1** | 🔒 | This is a **wide-audience product**, not a personal rig. Generic (language/framework-agnostic) rules are load-bearing; the enforceable content lives in **R9**. | 2026-07-11. Resolves the "personal tool vs product" straddle in favour of *product*. *(Rebuild R24: "independent **per-plugin** installability" is now moot — one `companion` plugin.)* **Reworded 2026-07-17:** dropped "README discoverability is load-bearing" — it contradicts the owner's "README is never loaded" and the Claude-facing / CLI-only doctrine (nobody browses these docs by hand). The product + generic-rules framing stands. |
| **R2** | 🔒 | A **single Requirements Ledger** (this file) is the source of truth for durable requirements; CLAUDE.md and ROADMAP reference it by ID. | 2026-07-11. Fixes the old five-surface scatter (achieved: charter's decisions surface was deleted in the rebuild; CLAUDE.md + ROADMAP now point here). |
| **R3** | 🔓 | Token efficiency is a **core value**, but is no longer enforced as a per-hook character-budget NFR. | 2026-07-11, **reshaped by the rebuild (R24)**: `tests/token-budget.bats` and the per-hook ratchets were **retired** — that apparatus defended a cost the steering-doc-read-once-per-session model doesn't incur, and it drove prose into cryptic, unmaintainable anchors. Efficiency now means "the steering doc stays lean," not a CI char-count. **Clarified 2026-07-17:** the 🔒→🔓 downgrade scopes to the **enforcement mechanism** (the per-hook char-budget NFR is retired) — **not** the value. Token efficiency remains the **core lens** (owner: "it's THE driving principle"; R50 calls the steering injection "the one output with real token cost"); only its CI ratchet was dropped. |
| **R4** | ⚰️ | ~~De-duplicate cross-plugin code via a build step → four installable plugins~~ | Retired 2026-07-11 by the rebuild (**R24**): there are no longer *four* plugins to de-duplicate across — one `companion` plugin means no cross-plugin duplication and **no build step**. The problem R4 solved was dissolved, not solved. |
| **R5** | 🔒 | Recommendations default to **brutal honesty + multiple-choice**, and **must name the R-IDs / architecture a recommendation touches or would change.** | 2026-07-11. Promotes the 2026-07-01 "a requirement conflict is a surfaced trade-off, names the requirement it would retire" clause from a buried rule to the default interaction. Extends it from *decisions* to *this ledger*. **Canonical home for the recommendation / brutal-honesty contract (2026-07-17, per R2 one-canonical-home):** **R17** (critique posture), **R49** (its decision-shaped *park-the-full-payload* refinement), and **R51·3 / R52·1** (the default-interaction-shape + nudge extension) all **operationalize R5 and carry no independent authority** — they cite R5, they don't restate it. |

## Durable requirements & decisions (migrated 2026-07-11, per R2)

The hard invariants (from CLAUDE.md) and durable design decisions (from ROADMAP) now live
here as the canonical status record. CLAUDE.md keeps the invariant *statements* (it's the
only auto-loaded doc — R3), tagged with these R-IDs; ROADMAP's decision sections became
pointers here. Blow-by-blow history stays in `git log`.

| ID | Status | Requirement | Rationale · Retires · Supersedes |
|---|---|---|---|
| **R6** | 🔓 | The plugin is self-contained (Claude Code installs a plugin's subdir alone, so it can only use files under its own root). | Old CLAUDE.md invariant #1. **Platform fact, not a defended decision** — the loader *enforces* this; no design choice here can violate it, so 🔒 was the wrong tier (downgraded 🔓 2026-07-17). Becomes a **live** concern only if **R52**'s `tq`-standalone extraction ships (then per-package self-containment matters again). *(Rebuild R24: one plugin, so there was never cross-plugin sharing to forbid.)* |
| **R7** | 🔒 | **Hooks are best-effort and must never break the triggering action *through error or failure*** (`set -uo pipefail`, swallow errors, exit 0 when silent). The **one intentional exception** is the R19/R43 secret gate's `exit 2` — a sanctioned block, not a failure. | CLAUDE.md invariant #2. A companion that breaks the user's action *by malfunctioning* is worse than one that stays silent. **Reworded 2026-07-17:** the old absolute "never break the action" literally contradicted the secret gate (the one hook designed to block a Write); the real rule is never break it *incidentally*. |
| **R8** | 🔒 | **`tq` is THE task queue** — the companion owns its own task store (`~/.claude/companion/tasks`, not `~/.claude/tasks`) and does **not** use Claude Code's native task tools. `tq add/doing/note/done/list/report` is the whole queue; each session dir is root-stamped so resume scopes to a repo with no native transcript. *(Amended 2026-07-23, **R69**: "the report fires on every state change" → mutations print a one-line counts delta; the full report fires at boundaries — done/import/report/session start.)* | 2026-07-11 (owner directive: *actively avoid native tasks*). Was a *fallback* for gated models; now the sole mechanism. Reverses the task-queue half of **R10** — see there. |
| **R9** | 🔒 | **Rules stay generic (wide audience)** — no hardcoded language/framework/ecosystem allowlists. Delegate *recognition* to the model; hardcode only *invocation* a hook can't avoid (prefer the project's own tool); detect *structure* generically. | CLAUDE.md invariant #5; the concrete mechanism behind **R1**. An allowlist rots and biases the suite to one audience. |
| **R10** | 🔒 | **Native-first for hooks, statusLine, AskUserQuestion, and permissions/`auto`** — use Claude Code's native mechanism there; hooks earn their keep only where they *execute* on an event or read state a session can't see. **Exception: NOT the task queue** — the companion owns its queue (**R8**). | ROADMAP durable decision. 2026-07-11: the "native task list" clause was **retired** at owner directive (actively avoid native tasks — they're gated off on the newest models and the queue should be self-owned and stable). Native-first still holds everywhere else. **Note (2026-07-17, R51):** "native-first for **AskUserQuestion** / statusLine" is now partly **platform-forced** — those are among the only three plugin-addressable surfaces, so native isn't a live *choice* there. The one genuinely contestable native-vs-own call left is the queue (**R8**). |
| **R11** | 🔓 | **Run in auto** — recommended owner `settings.json`: `permissions.defaultMode: "auto"` + a `deny` set + an `ask` set (force-push, `reset --hard`). | ROADMAP durable decision — the safe-autonomy posture the owner asked for. **Downgraded 🔓 2026-07-17:** this is **owner-environment config the plugin cannot enforce** (`/companion:setup` wires the status line, not `permissions`), so it's a recommended posture, not a plugin invariant. The binding part is the **reversible-only safety model** autopilot composes with (R26/R34); the deny/ask examples should be refreshed to ones that actually bite (`rm -rf /` is refused without `--no-preserve-root` anyway). |
| **R12** | 🔓 | **Proportionality over maximalism** — every practice scaled to the change's complexity/risk. | ROADMAP durable decision. **Downgraded 🔓 2026-07-17:** a **steering value** (lives verbatim in STEERING's clean-code clause — blast-radius, "~300 lines is a seam smell," YAGNI) and overlaps **R13**; by **R28** a judgment-value belongs in STEERING, not as a plugin-enforced 🔒 constraint. Kept as the ledger anchor for the value; not enforced. |
| **R13** | 🔒 | **Verification + simplicity over methodology labels** — encode the essence (SOLID/DDD/YAGNI, boring & reversible) as concrete generation-time rules (no-seam + deletion-test; unit-cohesion + complexity-altitude), **not** as methodology checkers (a "SOLID checker" isn't mechanically viable). | ROADMAP durable decision. |
| **R14** | 🔒 | **Non-technical-owner posture** — autonomy on the reversible, plain-language consent on the consequential (the line is reversibility + cost + data-safety); verification must be **observable** (demo it working, not read code). | ROADMAP durable decision. |
| **R15** | 🔓 | **Subtractive force + quiet injection** — inject the working agreement **once per session** (SessionStart), then on compaction re-anchor with queue + LESSONS + pointer (not the full doc, R30·d2/R32·3); *state* (carry-over, drift) is never suppressed, only policy prose. | ROADMAP durable decision; the mechanism behind **R3**'s per-session leanness. **Reworded + downgraded 🔓 2026-07-17:** the old text ("bootstrap-once via a `claude-companion` marker… re-anchor in **one line**") was pre-rebuild framing — the current mechanism is `session-start.sh` injecting STEERING once and re-anchoring at ~2.4k tokens on compaction, not one line. Serves the (now 🔓) R3 value; 🔓 to match. |
| **R16** | 🔓 | **Clean ≠ correct** — weigh new work against the recorded direction (this ledger + decisions + roadmap) before it lands, at both intent-time and outcome-time. | ROADMAP durable decision. **Downgraded 🔓 2026-07-17:** unverifiable steering, restated verbatim in STEERING ("weigh against recorded direction… at both intent-time and before done") and overlapping R5/R17; by **R28** a judgment clause lives in STEERING, not as an enforced 🔒. Kept as its ledger anchor. |
| **R17** | 🔓 | **Critique posture: selective, substantive-gated, bidirectional, self-challengeable** — EVALUATE before executing; challenge both recorded constraints *and* the owner's own accumulated bias; object only on real signal. The mandate itself stays challengeable. | ROADMAP durable decision (2026-06-19). **Folded into R5 2026-07-17:** R5 is the **canonical** contract home; R17's payload (bidirectional, self-challengeable, object-on-signal) is carried more sharply by R49, R52·3, and STEERING's always-on honest-verdict clause (R51·3). Kept as the origin anchor, downgraded 🔓 — **operationalizes R5, no independent authority.** |
| **R18** | ⚰️ | ~~**Decided against a charter doc-inventory state file** — chose the CI drift-guard instead.~~ | ROADMAP "decided against" (2026-06-01). **Retired 2026-07-17:** obsolete on every axis — "charter" was deleted by **R24**, the **CI drift-guard it endorsed was also deleted by R24** ("the drift-guard that policed self-imposed duplication — none of it verifiable"), and its forward-pointer was to **R4** (itself ⚰️). A 🔒 entry endorsing a deleted mechanism for a deleted subsystem cannot be in force. The underlying question, if it ever returns, belongs to R24/R52. |
| **R19** | 🔒 | **Decided against a hard plugin-owned destructive-action gate** — gating is **native** (`permissions.deny`/`ask` + `auto`). Narrow exception: the PreToolUse **secret floor** (`secret-guard.sh`) blocks a write, because native permissions scan commands/style but nothing scans file *content* for committed credentials, and a leaked key is irreversible. | ROADMAP "decided against" (2026-06-16; secret-floor exception 2026-06-21). **Reworded 2026-07-17:** dropped the dead 4-plugin name "tidy" → `secret-guard.sh`. The live surface of this exception is now carried by **R43** (all content tools), **R50** (feature flag), **R32·6** (generic-warn vs anchored-block); R19 remains the origin anchor. |
| **R20** | 🔒 | **Decided against native plan mode** for the present-before-work step — the task-queue's interpret→present→approve loop is used instead (plan mode is read-only + all-or-nothing per session; the owner wants to run in auto and review only the queue interpretation). | ROADMAP "decided against" (2026-06-16). |
| **R21** | 🔒 | **Decided against one single CLAUDE.md as the only doc** — use a few lean Claude-context files (CLAUDE.md + map + this ledger + LESSONS/GLOSSARY on demand). | ROADMAP "decided against" (2026-06-16). **Reworded 2026-07-17:** the recorded *rationale* was dead — "charter's separate-file detection" no longer exists (R24 deleted charter) and "per-plugin CONTRACTs" was 4-plugin-era (one plugin, one STEERING.md now). The *decision* still holds for a **current** reason: CLAUDE.md is the only auto-loaded doc (**R3**), so keep it lean and split the rest by responsibility (**R24**), loaded on demand. |
| **R22** | ⚰️ | ~~Decided against consolidating the 4 plugins into 1~~ | Fully reversed 2026-07-11 by the rebuild (**R24**): consolidation to **one** plugin actually happened — going further than R4's "shared source, four installables." The 2026-05-31/-06-16 "delete first, then judge; revisit only if it bites" *did* bite. |
| **R23** | 🔒 | **Anti-rework floors** are bounded, disable-able, **detect-not-decide** gates that supply facts for the model to judge. | ROADMAP "prevention taxonomy". *(Rebuild R24: the only floor kept as **enforced code** is the PreToolUse **secret gate** — it blocks. The others (alignment, intent→outcome, regression) were **advisory** Stop-hook prose that the model could skip; they now live as guidance in `STEERING.md`, not as hooks — the honest home for un-enforceable steering.)* *(**R28** 2026-07-12 **vindicates** this: R27's brief attempt to deliver **intent→outcome** as a PostToolUse reminder hook was retired — it's pure STEERING prose again, exactly as this entry holds.)* |
| **R24** | 🔒 | **Architecture: one steering document + a tiny enforced core.** All steering (queue discipline, critique/recommendation posture, clean-code standards, autopilot) lives in **one** file (`plugins/companion/STEERING.md`), put in context once per session. Only behavior that must **block, inject, or guarantee control-flow** is code: the secret gate (block), session-start injection + cross-session resume (inject — session-boundary I/O the model can't self-perform), autopilot (control-flow), and the `tq` queue. | 2026-07-11 ground-up rebuild (owner-committed). The old system was prompt-injection middleware whose ~95% advisory prose was scattered across ~15 hooks and four plugins, defended by a token-budget NFR and a drift-guard that policed self-imposed duplication — none of it verifiable. Collapsed ~12,500 lines → a few hundred. Separates the reliable (enforced) from the ignorable (steering) so each is honest about what it is. Supersedes/reshapes **R1, R3, R4, R6, R22, R23**. **Amended 2026-07-17 by R52:** the *binding* content of R24 is the **discipline** — tiny enforced core, one steering doc, reliable-vs-ignorable kept separate, no seam-sprawl. The **single-plugin *packaging*** is **no longer a requirement** (downgraded to current-state fact): a deliberate future extraction (e.g. shipping `tq` standalone) is a fair, un-blocked option and does **not** reverse R24 — provided it honors the anti-sprawl principle. What R24 forbids is *unmotivated* fragmentation, not packaging per se. |

| **R25** | ⚰️ | ~~**Clean-as-you-touch is a real hook — format-only** (reshaped by **R28**). A PostToolUse[Write\|Edit] pass (`bin/touch.sh`) formats the edited file with the project's own formatter — the one genuine *execution* worth a hook.~~ | 2026-07-11 (owner directive, after the rebuild); **reshaped 2026-07-12 by R28**; **retired 2026-07-17 by R51.** Originally the hook also *surfaced blast radius and flagged over-budget size*; R28 moved those nudges to STEERING and left `touch.sh` doing only the format. **R51 then retired `touch.sh` entirely** (owner directive to move away from advisory hooks): formatting is a low-stakes, reversible convenience — a *nudge*, not an irreversible guarantee — so it belongs in STEERING like every other judgment call, leaving the enforced core with **no *execute* hook at all** (only block / inject / control-flow). Whole-project cleanliness sweeps remain in `/companion:advise`. |

| **R26** | 🔒 | **Autopilot is enforced + persisted, not just steering.** A per-repo flag (`/companion:autopilot on\|off`, `bin/autopilot.sh`) survives restarts; while on, the **Stop hook** (`stop-autopilot.sh`) auto-continues the queue until only ❓/⏳ remain (no-progress capped) and the **ask-guard** (`ask-guard.sh`, PreToolUse[AskUserQuestion]) blocks asking. The status line shows ✈️. The **design-preview** was restored to STEERING as the wireframe convention (`╔═╗`/`▒`/`█`), steering-only — no gate. *(R27 briefly made it an enforced block; **R28** 2026-07-12 retired that — design-preview is steering-only again, as this entry always had it.)* | 2026-07-11 (owner directive). Two of the five capabilities the owner checked were advisory-only after the rebuild; autopilot was pulled back to **enforced** (a second conscious partial-reversal of R24, after R25), design-review to a **richer steering** clause. The enforcement machinery (a Stop hook + a PreToolUse deny + a persisted flag) is deliberately re-added — the owner values the guarantee. |

| **R27** | ⚰️ | ~~Two enforced work-gates (design-preview + return-review) + one advisory intent→outcome reminder, as PreToolUse/PostToolUse/UserPromptSubmit hooks.~~ | Retired 2026-07-12 by **R28** (owner directive), one day after it landed. The gates enforce *workflow judgment* and the reminder is a *nudge* — both belong in STEERING under the R28 principle, not in hooks. Deleted `work-guard.sh`, `prompt.sh`, `intent-note.sh`; stripped the gate helpers (`has_parked`, `*_flag`, `looks_visual`, `decisions_path`) from `lib/companion.sh`; removed the 🎨/🔒 status-line icons; reverted `ask-guard.sh` to autopilot-only. The three clauses live on in STEERING as prose (they always did — the gates were redundant enforcement). A worked example of the thrash R28 exists to stop. |

| **R28** | 🔒 | **The hook/steering boundary is decided by a component's *nature*, not case-by-case.** Enforced **code** ONLY for what must **block** (the secret gate's `exit 2`), **guarantee control-flow** (autopilot), or perform **session-boundary I/O the model can't do itself** (session-start injection + resume). Everything that is **judgment** (wireframe-first, weigh-against-recorded-direction, present-parked-first) or an **advisory nudge** (blast-radius, size, outcome-recap, formatting) is **STEERING** — prose the model reads once per session and applies. The deciding rule: *block / control-flow / session-boundary-I/O → hook; judgment or nudge → steering.* | 2026-07-12 (owner directive). Re-affirms the **R24** line after the R25/R27 drift bolted hooks onto nudges and workflow-judgment (and after a session of thrash — one gate flipped block→advisory→steering in an afternoon). **Retires R27** (design/return gates + intent reminder → STEERING), **reshapes R25** (touch is format-only; blast/size → STEERING), and **vindicates R23** (intent→outcome was always un-enforceable steering). **R26 autopilot stays enforced** — a control-flow guarantee against the model's own helpfulness bias is neither a nudge nor mere judgment. The rule exists to end the re-litigation, not to be re-argued per feature. **Reworded 2026-07-17 (R51/R52 sweep):** the old text named "**execute** (the formatter pass)" as a category — but **R51 deleted the formatter pass**, leaving *execute* with no members, so the stale example was actively misleading a reader of THE dividing rule. Recast as **block / control-flow / session-boundary-I/O**: "inject" is **not enforcement** (it delivers the *ignorable* steering layer) — its honest justification is session-boundary I/O the model can't self-perform, not that it "guarantees" anything. The **principle is unchanged and stays locked**; only the category scheme + dead example were fixed. |

| **R29** | 🔒 | **`/companion:advise` — a repeatable brutal-honesty critique ritual.** A command (not a hook — pure judgment/workflow per **R28**) that takes a target (file / subsystem / decision / free-text topic; default: the whole project), spawns an **independent critic panel** (distinct lenses — correctness · simplicity · UX · cost · steelman-then-attack) so the critique comes from contexts that *didn't build the thing*, synthesizes deltas, and presents each as a **recommendation-first `AskUserQuestion`, one at a time**, then closes the loop into `tq` + an offered ledger entry. Every critic is licensed to conclude "already right — no change." | 2026-07-12 (owner directive). Operationalizes the **R5** brutal-honest recommendation posture and the **R17** critique mandate as an on-demand ritual, and applies the **R28** independence insight (an outside context can't rubber-stamp what it didn't build). A manufactured delta would be the fake pushback R5/R17 forbid, so "no change" is an allowed, encouraged verdict. Owner-present by design — the ask-guard blocks it under autopilot. |

| **R30** | 🔓 | **Claude-first refinements — 9 owner-decided deltas (all built, 1.7.0–1.9.0).** A ground-up-redesign pass (goal: "designed primarily for the agent that runs it, similar UX + code quality") produced 9 selected changes: **(1)** `tq` tasks gain a `done-when` acceptance field; **(2)** re-inject the queue on context compaction (`PreCompact` + `SessionStart[compact]`); **(3)** STEERING → hybrid *checklist-header + prose*; **(4)** formatter = detect the project's own command (pre-commit / package.json / Make), fall back to the per-ext table; **(5)** `/companion:audit` → multi-agent workflow; **(6)** challenge posture → a required *risk / what-changes / R-IDs* slot in `ship-it` **plus** a devil's-advocate sub-agent for consequential changes; **(7)** a curated, model-maintained `LESSONS.md` injected at SessionStart; **(8)** a strict CI lane (bash-3.2 · `set -u` · no formatters · no git identity) + a hook-fuzz test; **(9)** the status-line beacon animates *on activity only* (static, no timer, when idle). | 2026-07-12 (owner directive, decided via a one-at-a-time recommendation menu). Recorded per **R5**; **build pending and sequenced in batches** — several touch hooks/schema/CI, so not one mega-change. Deltas 3/6/7 sharpen **R5/R17/R28**; 4 sharpens **R9**; 8 is the CI-robustness lesson from the 1.4.x macOS/jq bugs. Each delta ships with its own tests + docs; this entry tracks the set until built, then splits into per-delta entries or updates in place. 🔓 while unbuilt — fair to re-sequence or drop a delta. *(**All 9 built + shipped** across 1.7.0–1.9.0: Batch 1 — d7 `LESSONS.md` · d9 activity-only beacon · d4 formatter-detect; Batch 2 — d1 `done-when` · d3 STEERING checklists · d8 CI fuzz + strict lane; Batch 3 — d2 compaction re-anchor · d6 challenge slot + devil's-advocate · d5 audit sub-agent panel. Now shipped.)* **Demoted to 🔓 as a build-tracker (2026-07-17):** R30 promised to "split into per-delta entries" once built and never did — it is a 9-item **changelog frozen at 🔒**, and several sub-deltas were since reversed/refined elsewhere (d2's PreCompact half + d4 + d5 + d9 by **R32**; d1's render half by **R47**). Its live survivors (d3 STEERING format, d6 challenge slot, d7 LESSONS, d8 CI lane) are folded into STEERING/R48 — treat those as authoritative, not this bundle. Kept 🔓 for history, non-binding. |
| **R31** | 🔒 | **Playtests are autopilot-conditional.** Autopilot **ON** → never raise a playtest (the owner is away); capture the need as a `⏳ [blocked] playtest: <what>` task, silent, resurfaced on return with the parked pile. Autopilot **OFF** → offer a quick playtest when the change has a human-observable surface (UI / CLI flow / visible behavior). | 2026-07-12 (owner requirement). **Reshapes** the prior *"a human playtest is the one thing never parked — note it, keep going"* clause: under autopilot it now becomes a `⏳` (silent, resurfaces) instead of a mid-flight "playtest pending" note. STEERING, not a hook — there's no tool call to gate model output, so per **R28** this is prose, not machinery. |

| **R32** | 🔓 | **`advise` self-critique bundle — 9 owner-picked fixes, all shipped in 2.0.0.** d1 statusline `IFS=$'\t'` (confirmed bug → R46) · d2 `touch.sh` per-ext only · d3 compaction re-inject = queue+LESSONS+pointer, not full STEERING · d4 delete `pre-compact.sh` · d5 refreshInterval 1→3 · d6 secret-guard generic rule → warn, anchored keys still block (→ R43/R50) · d7 `tq cancel` · d8 audit merged into advise · d9 README polish. | 2026-07-12, via `advise` dogfooding itself. Demoted 🔓 2026-07-17: a shipped changelog, not a constraint — durable survivors promoted (R46, R43/R50); d2 voided by R51 (`touch.sh` deleted). History, non-binding; R46/R43/R50/R51 are authoritative. |

| **R33** | 🔒 | **Under autopilot, visual/design/direction choices are parked, not auto-decided.** A taste / design / direction / wording choice is the owner's call **even when trivially reversible** — the autopilot loop parks it (`❓ [parked] <choice + options + recommendation>`) and surfaces it on return, instead of auto-resolving it under the "decide routine reversible calls yourself" rule. This holds especially when the choice arises from something the owner *asked for while autopilot is on* (that's theirs to answer). Lives in STEERING's autopilot clause + the `ask-guard` deny message. | 2026-07-12 (owner requirement, prompted by autopilot auto-picking a status-line icon it should have parked). **Refines R26**: reversibility was the only auto-decide axis; *ownership of taste* was the missing one. STEERING + a hook-message tweak, not new enforced logic — "which kind of decision is this?" is judgment (R28). **Amended 2026-07-19 (R59):** this always-park-taste rule is **overridden while `decisive` mode is explicitly on** — the owner opts into having autopilot auto-pick its recommended option for *reversible* choices, taste included. R33 stays the **default** (decisive off); the carve-out is opt-in + audited (every auto-pick recorded) + never applies to the irreversible. Surfaced as a visible amendment (R5), not silent. |

| **R34** | 🔒 | **Ship-mode: autopilot auto-commits to a work branch; the status line leads with the plugin-state cluster.** **(a)** A per-repo `ship` flag (`/companion:autopilot ship on\|off`) — while autopilot is on, the Stop hook commits each turn's accumulated work as a reversible checkpoint on an `autopilot/*` branch (auto-created off the default to protect it; **never the default, never a push**), for the owner to review + `/companion:ship-it` on return. Best-effort; falls back to a companion git identity if the repo has none. Status shows 📦 when armed. **(b)** *(status-line ordering — a separate, cosmetic concern, split out + downgraded 🔓 2026-07-17: the signature cluster `📋/❓/⏳ · ✈️ · 📦` leads, after the beacon+shield glance, before generic model/tokens/git. Bundled here only because it shipped the same day — it has nothing to do with ship-mode.)* | 2026-07-12 (owner directive). The **safe subset** of "auto-ship under autopilot": the owner rejected auto-push/merge (it breaks the reversible-only safety model — R14 consent-on-the-consequential, R11 gating), so autopilot only **commits** (reversible); the push/merge stays the owner's manual `ship-it`. Composes with **R35**. **Known behavior:** commits to the *current* branch if it isn't the default (assumes you enabled ship-mode on a work branch); one commit per turn-with-changes, not per `tq done` (captures WIP too, squashed on ship); and it **skips** the checkpoint if the staged diff carries a high-confidence credential shape (`git add` isn't seen by the secret gate, so ship-mode re-checks — `lib` `companion_secret_re`). |
| **R35** | 🔒 | **`/companion:ship-it` merges to the default branch and prunes merged branches.** After a *successful* merge it deletes the shipped branch (local `git branch -d` — **refuses unmerged**; remote `push --delete` if it exists) and prunes other branches already merged into the default (`git branch --merged`, `-d` not `-D`, never the default/current), plus `git fetch --prune` for stale tracking refs. **Guardrail:** on a **shared** repo it *lists* merged remote branches and confirms rather than mass-deleting — a teammate's merged branch may still be wanted. | 2026-07-12 (owner directive). Extends ship-it's integrate step; the aggressive-prune option was chosen with the footgun flagged, so it's **safe-by-construction** (`-d` not force, default protected, shared-repo confirm). Composes with **R34**'s `autopilot/*` branches — the natural "autopilot accumulates → owner ships + cleans up" loop. **Note 2026-07-17:** the git-flag granularity (`-d` vs `-D`, `--prune`) is implementation and lives in `commands/ship-it.md`; the durable *decision* here is aggressive-prune, default-protected, shared-repo-confirm. |

| **R36** | 🔒 | **Autopilot is "keep-going" mode, not "owner-away".** It means *keep draining the queue without stopping* — the owner may be **present**, queuing up more tasks and keeping it on deliberately; the point is momentum, not absence. Behaviors are **unchanged** (don't stop to ask; park `❓` decisions + design/taste choices per **R33**; `⏳` owner-actions; reversible work only, binding/irreversible parked; a playtest → `⏳`) — but the framing and language drop "the owner is away / when they return." The owner reviews parked items whenever they check in; the pile is presented recommendation-first when autopilot is turned off. | 2026-07-12 (owner directive). Reshapes the framing of **R26 / R31 / R33**: it removes the present-vs-absent premise that *created* the R33 tension — there is no presence to detect if autopilot never assumes absence. The reversibility line (**R14**) still gates binding actions; that is about reversibility, not presence. Also fixed the R34 gap the owner asked for: ship-mode now **skips** a checkpoint whose staged diff carries a high-confidence key shape (`lib` `companion_secret_re`; `secret-guard` keeps its own inline copy so the gate never gains a fail-open dependency). |
| **R37** | 🔓 | **Domain-glossary layer — a Claude-facing `GLOSSARY.md` of coined terms, consulted on demand.** A project the companion governs keeps a terse `docs/GLOSSARY.md` mapping a **coined domain term → its meaning** (e.g. *"materialization cascade" for "when a lesson inside a section is made real"*). Claude **coins** a term when a concept recurs and its plain description is long, and **consults/reuses** it when naming code and writing docs — so one word carries twenty and naming stays consistent. It is **steering**, not a hook (R28: coining/consulting is judgment) and **on-demand**, not injected at SessionStart — it joins the map + ledger + stack-notes as part of a *self-describing project* (STEERING "How we know the project"), loaded when domain/naming work needs it, keeping session-start injection flat. Gotchas stay in `LESSONS.md` (R30·d7), decisions in this ledger, work in the queue — the glossary is *vocabulary only*. | 2026-07-13 (owner directive; the one transferable idea mined from mattpocock/skills' `CONTEXT.md`, the rest rejected as re-fragmenting R24 or contradicting run-in-auto/`tq`/generic). **Serves R3** (a shared vocabulary is a direct token-efficiency lever — its whole point), **generic per R9** (the model coins the terms; nothing is hardcoded), **steering-not-hook per R28**, and extends the **R24/R16** self-describing-project set beside `LESSONS.md`. Adds a convention; **reshapes/retires nothing**. On-demand (not a new always-injected doc) was the deliberate choice so it can't erode R3. **Downgraded 🔓 2026-07-17:** by its own words it "**reshapes/retires nothing**" — a purely additive, unenforced, on-demand convention has no constraint to defend, so 🔒 was the wrong tier. Token-harmless (on-demand), which is why it's fine to keep at all. |
| **R38** | 🔒 | **Autopilot-off starts the parked-pile review (`/companion:review`).** Walk ❓+⏳ one at a time, recommendation-first (parked items carry options+rec; blocked → done/still-blocked/drop); write each pick back to `tq` BEFORE new work; defer/bail always allowed (a forced march trains avoiding the off-switch); clean no-op when empty; scope = parked+blocked ONLY (open tasks need doing, not deciding). Two triggers: the autopilot-off command AND the STEERING conversational-off clause. | 2026-07-13. Command-not-hook (R28); reuses the R29 loop. The LOCK is the ritual + triggers, not the label — label history: merged into `resume` 2026-07-18, re-split 2026-07-19 (owner chose the pickup-vs-triage seam over less surface). |
| **R39** | 🔒 | **Resume is a triage handoff.** (A) `resume.sh` clears autopilot FIRST — announced when it was on (never silent-clobber a persisted intent; re-arm is manual); (B) STEERING documents the resume→off→park→review loop; (D) reinstatement PRESERVES each task's ❓/⏳/📋 class — a parked decision is never promoted to plain open (the triage-vs-drain distinction lives in the task's TYPE, so it survives compaction and holds in either mode); then hand off to `/companion:review`. | 2026-07-13 ("don't autopilot the answers"). Insight: autopiloting happens on the NEXT drain — fix by type, not mode flag. Rejected: an ask-guard exemption (R27/R28 anti-pattern). Command folded into review 2026-07-17, re-split as pickup-only 2026-07-19; all three behaviors + checks preserved throughout. |
| **R40** | 🔓 | **`ship-it` output is review-optimized.** (A) self-documenting messages — What/Why/R-IDs/tasks/test-result — ~90% of the value, the durable ask; (B) curate `autopilot/*` checkpoints into logical commits (`reset --soft` + regroup; no `rebase -i` here) — best-effort, fall back freely to one well-messaged commit (flagged over-built 2026-07-17); (C) structured PR body; (D-lite) right-size nudge, not a stacked-PR machine. | 2026-07-13 ("clean even if no human reviews"). Reshapes R34's squash (a blob is the hardest review); generic per R9; steering per R28. 🔓: output format is spec, not constraint. |
| **R41** | 🔓 | **`/companion:docs` (renamed from `document` 2026-07-22) — ground the ledger from an existing repo, tiered check › 🔒 › 🔓 › dropped.** Two-pass: silent ranked candidate report → owner triages survivors one at a time, Claude's candidate whys AS the options (+ always an open-ended own-rationale, no-clear-why→🔓, drop, defer). Only the owner's active pick records (anti-laundering); strength-of-why sets 🔒/🔓; provenance `stated`/`inferred`/`unknown`; a claimed 🔒 is challenged once; mechanizable constraints also emit a gate assertion (the tier Claude can't ignore). Producer side of advise. | 2026-07-13. Honesty principles extracted to **R53** (🔒) — this entry is the command spec (🔓). Reuses R29/R38 loop; generic R9; command-not-hook R28; serves R3. |
| **R42** | 🔓 | **Parked/blocked (`❓`/`⏳`) is a prefix-view over `pending` tasks, never a `status` value.** State lives *only* as a leading emoji on a `pending` task's subject; ~5 readers re-derive it (`tq`, `stop-autopilot`, `statusline`, and `companion_open_tasks` implicitly). Keep it a prefix — do **not** promote parked/blocked to first-class `status` enum values, and do not change the glyphs in one reader without the others. | 2026-07-13 — surfaced by `/companion:document` (R41), provenance `inferred` (owner-confirmed). **Why:** one `pending`/`in_progress` query then serves *both* the active drain and cross-session resume (**R24** subtract-as-you-add), and it keeps the `status` set simple; the reader duplication is the price. Promoting to a `status` value would make `companion_open_tasks` (selects `pending`/`in_progress`) stop resurfacing parked items on resume and break **R39**'s "classification survives compaction." A bats check pins reader behavior. **Downgraded 🔓 2026-07-17:** this is the *implementation* of **R39**'s "classification survives on the prefix," not an independent decision, and the bats check is the real guard. Merged conceptually into R39; kept 🔓 as the implementation note. |
| **R43** | 🔒 | **The secret gate covers every content-writing tool — no bypass.** A credential written through *any* content-writing tool must hit `secret-guard`. *(The specific wiring — `Write\|Edit\|NotebookEdit`, reading `.content // .new_string // .new_source` — lives as a code comment; the durable requirement is the no-bypass principle, since tool names change — 2026-07-17.)* | 2026-07-13 — surfaced by `/companion:document` (R41). **Why:** the gate is the one hard, irreversible guarantee (a committed key can't be un-leaked); a tool it doesn't match is a silent hole (ship-mode's staged re-scan is only a backstop, and only when armed). Closed the `NotebookEdit` (`.new_source`) gap the scan found. **Mild R9 tension** accepted: matching a tool *name* is invocation, not language recognition. |
| **R44** | 🔓 | **`tq` writes are atomic (temp file + `mv`), never in-place.** Every task mutation goes to `.$id.tmp` then renames over `$id.json`. Do **not** "simplify" to `jq … > "$f"`. | 2026-07-13 — surfaced by `/companion:document` (R41), provenance `inferred` (owner-confirmed). **Why:** atomic rename means a task file is never half-written if jq errors or the process dies mid-write — which is what protects crash-resume (breadcrumbs + `done_when` surviving a crash, **R30·d1**); jq can't edit in place, so temp+rename is the crash-safe form. Now carries a code comment so it doesn't read as removable idiom. **Downgraded 🔓 2026-07-17 (now guarded by a check):** per R41's own tiering (check › 🔒 › 🔓), a *testable* invariant should be a **check**, not 🔒 prose. Added a bats regression guard that the temp-file+`mv` idiom stays in `tq` (catches a "simplify to `jq > $f`" refactor); with that + the code comment the 🔒 prose is redundant. Kept 🔓 as the rationale anchor. |
| **R45** | ⚰️ | ~~**`stop-autopilot.sh`'s second default-branch check (after `checkout -b`) is deliberate defense-in-depth, not dead code.**~~ The re-read + re-check of the current branch before ship-mode commits must stay. | 2026-07-13 — surfaced by `/companion:document` (R41), provenance `inferred` (owner-confirmed). **Why:** it is the last floor on **R34**'s absolute "never commit to the default branch." If any edge case (a failed `checkout -b`, detached HEAD, empty `rev-parse`) left HEAD on the default, ship-mode would `git commit` straight onto `main`. Cheap insurance on a near-irreversible invariant. The "never commit to default" outcome is exercised from both entry paths (HEAD-on-`main`, pre-existing; and detached-HEAD → the checkpoint lands off-default with `main` untouched). The **second** guard specifically only fires in a state `checkout -b` can't reproduce (its own failure), so it isn't cleanly unit-testable — it's protected by its inline `# NEVER commit to default` comment + this entry. Don't delete it as "dead code." **Retired from the ledger 2026-07-17:** a "don't refactor this line" note about one defensive line is a code-comment/LESSONS concern, not a durable *decision* (a 🔒 per defensive line is unbounded growth). The line **stays** (its comment protects it); the durable requirement is **R34**, still exercised by the R45 bats test. |
| **R46** | ⚰️ | ~~**Tab-delimited `read`s use `IFS=$'\t'` — repo-wide.**~~ Any `read` splitting a tab-joined jq line whose last field is free text (a subject) must set `IFS=$'\t'`. | 2026-07-13 — surfaced by `/companion:document` (R41). **Why:** the trailing subject can carry spaces; a default-IFS split corrupts it — the confirmed **R32·1** space-in-value bug the status line already hit. `stop-autopilot.sh`'s counts-parse silently relied on numeric leading fields; hardened to match the `statusline.sh` convention. **No dedicated test:** with the subject as the trailing `read` field the default-IFS split still works today, so a test would pass with *or* without the fix (non-discriminating). Protected by the `IFS` change + the inline code comment + this entry; the existing `stop-autopilot` tests cover regressions. **Retired from the ledger 2026-07-17 → moved to `LESSONS.md`:** a repo-wide coding-convention "watch this trap" is LESSONS content, not a decision, and it was triple-homed (code + comment + ledger). The `IFS` change + comments stay; one `LESSONS.md` line now carries the gotcha. |

| **R47** | 🔓 | **`tq report` is Design-D compact, and it never renders `done_when`.** The report is: a glyph-count header (`📋 ▸n ◻n ❓n ⏳n ✔n`), **one line per active task** (in-progress/open/parked/blocked, leading `❓`/`⏳` stripped since the glyph moves to the front), **completed shown as a count only** (no `✔ #id` lines), and a trailing **`→ next: #id`** pointer = the in-progress task, else the head of the open queue (empty when neither exists). `done_when` is **stored** on the task and **re-surfaced on cross-session resume** (`lib/companion.sh`) but is **not** printed in the live report. | 2026-07-14 (owner directive, picked from a 7-layout menu — "D", then "more icons" reverted). **Why:** the report reprints on every `add`/`doing`/`done`, so its height is paid constantly; the per-task `done_when` sub-line doubled it — trimming it keeps **crash-resume intact** (`session-start.sh` on `source:compact` still renders it via the resume renderer, **R30·d1** unbroken). **Accepted trade-off (surfaced by the ship-it devil's-advocate):** `done_when` is now *also* invisible **mid-session**, including at the moment `tq done` fires — so the acceptance test can't be re-read against at completion time; there is deliberately **no** `tq show`/re-display path (that would re-add the height D removed). If that bites in practice, the fix is a *separate on-demand* `tq show <id>`, not putting `done_when` back in the always-printed report. The `→ next` pointer is a **mechanical** queue-order default (STEERING says override it aloud when blast-radius/dependency judgment disagrees — not a hook, **R28**). **Reverses R30·d1's *render* half only** (the `done_when` **field** and its acceptance-test role are unchanged; only its appearance in `tq report` is removed). Generic per **R9** (pure formatting). **Downgraded 🔓 2026-07-17:** a CLI report *layout* is the most reversible, taste-driven surface here (and by **R33** a wording/design choice is the owner's to revisit freely); it already churned once. The recorded trade-off (done_when invisible at done-time) stays as rationale; 🔒 over-constrained a display detail. **Amended by R69 (2026-07-23):** the *cadence* ("reprints on every add/doing/done") moved to delta-on-mutation + full-report-at-boundaries; the Design-D *layout* of the full report is unchanged. |
| **R48** | 🔒 | **The test suite is the executable half of the enforced core — keep green, don't grow.** ~30 bats cases + `check.sh` validators guard irreversible/un-eyeball-able behavior (secret gate, hook-fuzz never-break-the-edit, autopilot control-flow + no-spin, ship-never-default, cross-repo scope). Policy: (a) an intentional behavior change reddening an assertion → loosen it to behavioral, never pin cosmetics; (b) no new format-detail tests per feature — acceptance lives in `--done`, verify by exercising; test-authoring deferred. | 2026-07-14, reconciled after an audit showed the suite is enforcement, not theater (blanket rip-out rejected; 4,500 stale .bats lines removed). Reshapes "verify with check.sh" into keep-green-don't-grow; composes R28. |
| **R49** | 🔒 | **The recommendation contract: a decision-shaped ask is owed options, never a flat answer.** 2–4 genuinely different options, each naming cost/what-changes, recommended first + brutal-honest read (incl. "don't"). Identical under autopilot: off → ask live; on → park the FULL payload in the ❓ subject (options+rec, read back via non-truncating `tq list`). A thin answer or one-line park = bug (rubber-stamp review). Exception: the decompose class parks risk+questions, not options (**R65**). | 2026-07-14, two linked complaints with one root: advisory posture gets skipped. Operationalizes **R5**; the distinct durable clause is park-the-full-payload. Judgment not keyword (R9); not hard-enforceable — per-turn injection rejected (token cost). Amended by **R59**: decisive flips park→decide-and-record for reversible only. |
| **R50** | 🔒 | **Per-repo flags gate the ENFORCED core; the fail-safe invariant is the lock: the secret gate never fails open via a dependency.** `secret-guard` reads its flag inline (no lib source); only a literal `secret=off` disables; any read error leaves the gate ACTIVE; disabling warns loudly. State: one per-repo OFF-override file (absence = default on); resolution: env `CLAUDE_COMPANION_SECSCAN` (global/CI, wins) → per-repo flag → default. Steering keeps ONE knob (inject on/off) — flag-per-clause rejected (steering is ignorable-by-nature, R28). | 2026-07-16. Kept 🔒 2026-07-17: the invariant is the lock, the file format is revisable. Amended 2026-07-18: the `/companion:features` CLI + writers removed (UI wasn't earning surface; safe-direction — no one-command gate-off) — the flag mechanism + every enforced reader unchanged, checks green; cost: steering-off needs a hand-written flag. |
| **R51** | 🔒 | **No advisory hooks; recommendation-driven pick-from-CLI is the default interaction.** (1) `touch.sh` deleted (retires R25) — the enforced core has NO execute member: **block · inject · control-flow** only (sharpens R28). Accepted risk: formatting is now model-recall; the advise sweep is the fallback. (2) Reaffirms **R48** (spine kept; only touch.sh's tests removed). (3) Options-as-default-shape + context nudges (debt→task · blast→split · repetition→autopilot) + brutal-honest always — STEERING prose. Platform constraint: plugins cannot inject prompt chips — only SessionStart context, statusLine, `AskUserQuestion`. | 2026-07-17. "Remove all hooks" reconciled honestly: the nudge engine IS the injection hook. Composes R24 R28 R48 R49. |
| **R52** | 🔒 | **The product is the `propose → queue → drain` loop.** (1) Nudges are the funnel INTO the queue; `tq` is the durable spine (tested, crash-safe R44). Rejected: a scored nudge-hook (R28/R30·d2 anti-pattern) and a pack-split (the seams caused the sprawl, not the line count). Core stays block·inject·control-flow. (2) One-plugin *packaging* demoted to current-state fact (amends **R24**; the anti-sprawl principle stays locked). (3) Standing posture: challenge EVERYTHING — 🔒 means sign-off-to-change, never exempt-from-challenge; silence-by-deference is a miss. | 2026-07-17 owner directives. Licensed the ledger-wide sweep (reworded/retired ~25 entries; extracted R53). Composes R49/R51 (default shape), R50 (toggles = the modularity mechanism). |
| **R53** | 🔒 | **Ledger-honesty rules (extracted from R41): strength-of-why sets the lock · provenance sense · anti-laundering.** For **any** ledger entry (recorded by `/companion:document` or by hand): a real, articulated *why* → 🔒 (advise/challenge may not silently reverse it); a weak/unknown *why* → 🔓 (fair to challenge). Each entry reflects a provenance sense (`stated` / `inferred` / `unknown`) — a labelled `unknown → 🔓` is **preferred** over a confident-but-wrong 🔒, since confident-and-wrong is the worst input to an agent. **Anti-laundering:** only the owner's *active pick* records an entry; an unchosen assumption never becomes a 🔒. | 2026-07-17 — extracted from **R41** (the ledger audit found R41 bundled durable *principles* with a command *spec*). These are the load-bearing, generalizable honesty rules; the `/companion:document` mechanics that apply them are the spec (R41, now 🔓). **Composes with R5** (name what a recommendation changes) and **R52·3** (a 🔒 must survive challenge, so its *why* must be real). |
| **R54** | 🔓 | **Direction: contract-preserving regenerative `advise` — build the invariant tier FIRST.** The north star (owner-agreed) is that `/companion:advise`'s objective becomes *preserve the contract, treat everything else as disposable and fair to redesign from the ground up.* **The contract has THREE pillars, not two:** **(a) the UX record** (what the user sees/experiences), **(b) agreed NFRs / quality attributes** (only those the owner intentionally reviewed and agreed — R53 anti-laundering), and **(c) safety/correctness invariants as executable *checks*** — the things the user never sees but that must hold (secret gate fail-safe, `tq` atomic writes, autopilot-never-commits-default, cross-repo scope isolation). Pillar (c) is the one the naive "UX + NFRs only, ignore the rest" framing omits — and a ground-up regen that omits it *silently deletes* those invariants (a leaked key, a corrupted store). So "ignore everything else" is rejected in favour of "**unconstrained *except by the contract***": regen must reproduce the UX, meet the NFRs, and pass every invariant check. **Sequencing (owner-agreed, non-negotiable): checks first, THEN regen-by-default** — the invariant-check tier must exist and be proven before `advise` may default to regeneration; regen is a **confirmed, bounded, check-gated** operation, never a silent auto-rewrite. | 2026-07-17 (owner directive). **Why:** the principle is a clean generalization of **R53** (only intentionally-agreed → durable; everything else is disposable) and the R41 tiering (check › 🔒 › 🔓). But it only works if the *invisible* invariants are captured as checks — which is why this session's audit deliberately kept R44/R45/R50's fail-safes locked. **The safe-sequencing was reached after pushback:** the owner's first framing (UX+NFRs only, ignore the rest, regen by default *now*) was challenged as a footgun; the owner agreed to build the contract+invariant tier first. **Composes with R41** (`document` does the contract triage + tagging {UX-contract | agreed-NFR | safety-invariant | incidental}), **R48** (the test suite IS the invariant net), **R29** (advise), **R53** (anti-laundering). **Build-pending** (kept 🔓 per the R30/R32 lesson — no 🔒 build-bundle); the concrete pieces (UX record, invariant checks, advise reframe) earn their own entries when built. |
| **R55** | 🔓 | **Redesign logs UX + quality attributes ONLY; safety = executable checks; `/companion:redesign` = bounded, check-gated passes.** No technical-requirements catalogue (implementation is disposable); `INVARIANTS.md` is a pointer table to checks, not hand-maintained contract. Command history: advise sub-mode → standalone `regen`+`redesign` (2026-07-17, discoverability) → `regen` folded in as redesign's D3 per-module engine with `docs` required as D1 (2026-07-18; R1–R5 engine preserved verbatim, R56 command-gate moved to redesign.md). DA-hardened: autopilot-clear step-0 · D0 invariant-coverage gate · auto-revert-on-red · honest G3/G4 residual (owner-ack is the only guard there). | 2026-07-17, after pushback: rebuild stays gated on the invariant checks, in bounded passes. Composes R54 R41 R29 R48. |
| **R56** | 🔓 | **Complete the behavior net before trusting whole-app `redesign` — but the command prose has a hard ceiling.** `redesign`'s D0 gate correctly **stopped** a blind whole-app rebuild of this mature app: the net pins the *safety* invariants but not every *observable behavior* — proven by the statusline regen silently dropping the autopilot-beacon (a green rebuild that regressed the UX). Decision: **characterize the executable core** (`bin/`+`lib` — write behavior-pinning tests for the gaps, like the beacon test) **and structurally-guard the command prose** (each command prompt must retain its critical gate steps — a textual check like R44/G2). **The command layer is fundamentally NOT blind-regenerable** — a prompt's behavior is *Claude's judgment*, unverifiable by a check (R28: prose is ignorable-by-nature); the ceiling is structural guards + always bounded-regen-with-human-verify (R5). So whole-app `redesign` becomes safe for the **core**, never for the **commands**. | 2026-07-17 (owner directive, after trying `/companion:redesign` — D0 stopped it, owner chose "complete the net first"). Essentially the deferred **R48** test-authoring project, scoped by this ceiling. **Composes with R54/R55** (the net *is* the gate), **R48** (regression-gate — this backfills it), **R28** (prose = judgment = unverifiable). Build-pending, phased: **(1)** behavior-coverage audit of the core → gap list; **(2)** characterization tests for the gaps; **(3)** structural guards for the command prompts. **Phases 1–2 largely done (2026-07-23, via `/companion:cover`):** the audit found the core net **strong** (83 bats) — every `bin/` script's primary observable behavior guarded; only two candidate gaps surfaced, one a **false alarm** (LESSONS injection R30·d7 was already pinned by the session-start test's `GOTCHA_MARKER` assertion) and one **real+closed** (`ship.sh preflight` deepened to assert the R60 export wrote+carried the queue and the summary ran). Phase 3 (command-prose structural guard) shipped as the R56-P3 bats test. **Remaining:** whole-app `redesign` is still *unproven on a real run* — the gap now is exercising it, not missing core tests. |
| **R57** | 🔓 | **`ship-it` is contract-aware.** (a) name the contract impact in commit/PR (which pillar, which entries — loudest for UX); (b) propose the flow-spec edit recommendation-first, staged WITH the code in one commit — never a silent rewrite (the contract stays owner-governed source-of-truth; the drift check stays meaningful); (c) maintain the README docs index (README = the human doc, R66 exception). | 2026-07-18. Middle path picked over silent auto-rewrite (contract chases code) and hard-block-on-drift (friction). Composes R40 R54 R38/R49 R41; prose per R28/R56. |
| **R58** | 🔒 | **The living contract — accurate at any moment, as ONE plugin.** (a) ~~capture: UserPromptSubmit hook banks every prompt, write-only, ZERO injection [N1]~~ — **⚰️ RETIRED 2026-07-29, owner-picked from a 3-option menu after a measured audit.** Two facts killed it. **It had no consumer:** a full-repo grep for the store path, the `companion_captures_dir` helper and every prose reference found **zero readers** — not `cover`, not `docs`, not `advise`; STEERING's contract reflex is the model noticing *in conversation*, never a read of this file. **And it was costed on one axis only:** the clause above justifies it purely as `ZERO injection [N1]` — the token side — while the LOCAL side was never measured. Measured at retirement: **64ms and 13 process spawns on EVERY prompt, on the blocking UserPromptSubmit path** (it delayed the send), having accumulated 456KB of plaintext prompts across 12 repos — the liability R68 itself flagged. A slimmed one-`jq` version was prototyped (64ms→17ms) and declined in favour of deletion: paying anything for a log nothing reads is the wrong trade, and R23's anti-rework floor cuts the other way here — the store's *format* was never the hard part, so rebuilding it if a consumer is ever designed is cheap. Deleted: `bin/capture.sh`, the `UserPromptSubmit` registration, `companion_captures_dir`, 2 bats cases, 1 INVARIANTS row. **The banked data is left in place** (`~/.claude/companion/captures`) — it is the owner's, and deleting prompt history is not the plugin's call. (b)–(d) below stand unchanged; (b) contract reflex (STEERING): a UX/QA change moves the flow spec FIRST, code queued against it; (c) drift backstop `contract-drift.sh` — advisory warn, deliberately NOT a gate (contract-relevance is judgment; a gate false-positives into disablement); **amended 2026-07-22: runs at the SHIP boundary only** (ship-it's contract-sync step, no longer every check.sh run) — per-run firing on mid-work trees (where drift is the normal intermediate state) trains warning tune-out, and CI was a clean-tree no-op; closes the last R58 follow-up; (d) `/companion:cover` — the test arm (now scaffolds, R61). | 2026-07-19, provenance stated. Two-system split rejected: a second SessionStart injection breaks N1 and reverses R24/R52; the contract is inseparable from execution (ship-it syncs it, advise critiques it, redesign rebuilds against it). Composes R54 R57 R41 R48/R51. |

| **R59** | 🔒 | **Decisive mode — `/companion:autopilot decisive on\|off` (persisted per-repo, mirrors ship).** While autopilot+decisive: auto-pick the recommended option for REVERSIBLE decisions (taste included — overrides R33 for this mode only), record every pick (`tq note`/`add`), park only the irreversible-critical (push/delete/spend/binding/destructive; unsure → park). Delivered as STEERING + a mode-aware ask-guard message (deny still fires; guidance flips park→decide). Off by default; no-op unless autopilot on; shown ✈️⚡. | 2026-07-19, boundary + name owner-picked. Safety moved from stopping to AUDITABILITY (opt-in, breadcrumbs, review walks the picks); the irreversible still parks. Amends R33, R49; composes R26 R34 R36 R38/R39. Never auto-picks a `decompose:` park (R65 — not an options-park). |
| **R60** | 🔓 | **Portable queue over git.** `tq export` → path-free `.companion/queue.json` (open tasks, done-when + latest notes, ❓/⏳ class in subject); `tq import` re-stamps under the local identity; idempotent, dedups (subject,done_when) across ALL statuses (finished work never resurrects; distinct same-subject tasks not collapsed). Hardening: per-file filter (one corrupt file skipped-with-count, never zeroes the backlog) · import refuses a foreign-repo session · conflicted queue.json = LOUD no-op. Wired: resume imports first; ship-it exports + stages. CLI-not-hook per R28. | 2026-07-20; git-as-carrier + build-into-companion owner-picked. 🔓: linear handoff only — concurrent two-way is last-export-wins. Composes R44 R38/R39 R57 R63; repo-membership = the same match rule as `companion_open_tasks`. |
| **R61** | 🔓 | **Anti-drift gate: a flow's `- [E] \`name\`` Tests line must resolve (segment-matched) to a real bats `@test` title or the build FAILS; `[S]` judgment lines are skipped (honest gaps visible, not failed).** Gate + guard-test share ONE matcher; the guard proves reject-phantom AND accept-real. Honest ceiling: proves a test exists + is wired (CI proves it passes) — not golden correctness; never covers [S]. | 2026-07-20; spec-first+gated, black-box native-runner owner-picked. The lever is ENFORCEMENT (any advisory sync gets skipped; prevention > detection N7). Amends R58·d: cover now recommends-then-scaffolds (buy-in still precedes any write). Composes R66-shape R54 R44. |
| **R62** | ⚰️ | ~~**The UX/quality contract is human-collaboration-first: one readable flow page per journey, not machine tables.**~~ *Reversed by **R66** (2026-07-22): `docs/flows/` stays the contract home with the same section semantics + R61 tests grammar, but the shape is dense machine spec (`when·why·steps·quality·tests·changes`). The change-interface-is-the-conversation idea survives; only the human-readability premise died.* What R62 did while live (2026-07-20): retired `UX.md` mega-tables + `NFR.md` ceremony → per-flow pages + `_patterns.md` (define-once) + `_quality-bar.md` (N1–N7 floor); `INVARIANTS.md` untouched. | 2026-07-20 owner directive (human-in-loop discussion surface); honest trade recorded then: weaker regen input than dense tables — the exact axis R66 later flipped. Amended 2026-07-22 (Why line): each flow carries a one-line rationale + R-IDs; a standalone `FEATURES.md` handbook was offered and REJECTED (fourth home, drift-prone, against R2/R24) — the handbook is `docs/flows/` itself. Amends-history: R54 R58 R55 R2; composes R61. |
| **R63** | 🔓 | **Task-store scoping keys on a per-WORKING-TREE identity, not abspath.** `.repo` = random id at `git rev-parse --git-path companion-repo-id` (moves with the tree; distinct per worktree/clone/fork), matched first; legacy `.root` abspath fallback for old stores. Fixes moved-repo-loses-queue WITHOUT merging related trees. Additive, non-breaking. | 2026-07-20, ported from the Claude-only exploration (the one strictly-better piece). Mid-ship correction: a root-commit-SHA key would have MERGED worktree/clone queues (cross-project-bleed break for parallel worktrees) — re-keyed per-worktree, both cases tested. Cross-machine transport stays R60 export/import. Composes R60 R38/R39. |
| **R64** | 🔒 | **No plugin-generated file at a governed repo's root — the README (docs-index section, R57) is the sole exception.** Contract set lives in `docs/` (R41/R57), portable queue in `.companion/` (R60); the generated gate's default home is **`.companion/check.sh`** (rides the same commits as the queue — R60 carry unaffected). Governs only files the companion CREATES — a repo's own gate is used wherever it lives. Runner prose names the resolved gate, never a hardcoded root path. | 2026-07-22, owner-picked over everything-under-`.companion/` (would reverse R57/R62-era docs home). Composes R57 R60 R41; steering/prose only (R28). |
| **R65** | 🔒 | **The open queue stays minimal-blast; high-blast-for-missing-context work decompose-parks.** `📋 open` = pre-cleared (routine, reversible, verifiable — safe to drain unattended). High-blast-for-missing-context → `❓ [parked] decompose: <task> — risk: <analysis>; need: <questions>` — NOT an option-pick (options invented without the context are premature; refines R49 for this class). Review runs it as an interview; answers re-enter propose→queue→drain; original closed + replaced by minimal-blast children — never survives as high-blast open. Irreducible blast (push/migration/payment): owner blesses it through explicitly or keeps it ⏳. The drain never works a `decompose:` task. | 2026-07-22; decompose-park owner-picked over literal eject (breaks nothing-hides; no terminal case) and over an enforced drain gate (declined — available if the advisory form proves skippable). Composes R36 R38 R59; steering+command prose only (R28/N7). |

| **R66** | 🔒 | **Claude-only documents: every plugin-generated/maintained document is written for Claude as the consumer; `README.md` is the sole human-facing document.** Doc shape optimizes for agent density + parseability, not human readability. Executed as: CHANGELOG retired (git log + this ledger cover it) · flow contract → machine shape (reverses **R62**, whose why was humans-only) · ledger density per **R3** (compaction pass) · Why-lines kept as anti-reversal provenance, not "handbook" prose. **Not** in scope: the status line (owner's eyes, not a document), conversational surfaces (AskUserQuestion, recaps), README/R57 index. | 2026-07-22, provenance `stated` ("assume no human reads any documents; all outputs consumed by Claude except README"). STEERING trim offered, **declined** (motivated prose is why steering sticks — behavioral risk not worth ~25%). Composes: R3 (density), R1 (still wide-audience — *product* users are humans running it; their *docs* consumer is Claude), R57 (README exception), R61 (gate grammar preserved through the reshape). |

| **R67** | 🔒 | **Domain context arrives via natively-configured MCPs; the direction of truth is INWARD.** Orgs/individuals wire their own systems (wiki, tickets, schemas, design system) as MCP servers through Claude Code's native config — companion adds NO machinery and names NO systems (R9/N6). Steering reflex only: consult a configured domain tool before inferring; a decompose-park interview (R65) names when an answer likely lives behind one. External systems are *inputs*; what proves load-bearing is materialized into the repo's own record (ledger · flows · invariants) — the repo's contract stays the single source of truth. | 2026-07-22. An external-SOT registry (`docs/SOT.md`: domain → canonical external system) was designed, half-built, and **reverted the same day** (owner call): it created dual-truth ambiguity, collided head-on with **R8** (`tq` is THE queue vs "tickets are canonical"), and reversed the materialize-inward doctrine (R2/R41/R58). Composes N6 (native-first), R9 (generic), R65 (missing-context interviews), R2 (one canonical home). |

| **R68** | 🔒 | **Hook admission criteria — a hook must be local-only (no network), single-shot short-lived (no daemons/ongoing processes), bounded in blast radius (never push, never the default branch, never delete), and best-effort (R7).** ⚠️ **SCOPE, added 2026-07-29 — this entry governs SAFETY, not COST.** "Bounded blast radius" here means *destructive* blast, and "short-lived" was never measured. Read as a performance guarantee it is actively misleading: **this entry's own audit concluded "every existing hook passes the criteria" while `session-start.sh` was taking 2085ms and 358 process spawns on the owner's real store, growing without bound** (16108ms at 8x) on a path that fires at every session start and every compaction. That clean bill of health on the wrong axis is why the scan survived 80 numbered requirements. **Computational cost is governed by R81** (bounded work, measured in CI) — cite BOTH when admitting a hook. Sharpens R28/R51's block/inject/control-flow line with *how* a hook may run. Two sanctioned shapes at the edges: the secret gate's `exit 2` (a block IS its job) and autopilot's Stop hook (the per-invocation decision is ms-fast and no-spin-capped; the *ongoing* thing it sustains is the product's queue drain, not the hook). Preferred new-hook archetype: a content scan (PII/credentials), placed where it has teeth — at rest. Executed: `capture.sh` now redacts anchored credential/PII shapes (vendor-anchored keys, private-key blocks, SSN/card shapes — generic per R9, portable ERE) *before* banking, and rotates the store at ~1MB (closes the R58 follow-up "capture grows unbounded / plaintext secrets"). Audit result: every existing hook passes the criteria. | 2026-07-22 (owner directive: "hooks only if local, small blast radius, no large ongoing process; simple, short-lived, scan prompts for PII/security"). Challenged + reconciled: as worded it would kill autopilot (the ongoing run IS the pitch) and session-start (the steering engine) — the criteria bind the HOOK, not the workflow it governs; a live prompt-warn was offered and deferred (can't unsend; FP tune-out risk) in favor of at-rest redaction. A redaction test is R48-sanctioned (irreversible-harm class, un-eyeball-able). Composes R7 R9 R28 R51 R58. **AMENDED 2026-07-29 (owner-picked, 3-option menu) — the gate is widened, the stamp is refused, and the pass is the DEFAULT:** (a) `.companion/da-paths` had real holes — `hooks/hooks.json` (registering a hook), `tests/` (weakening a test IS 'a green gate certifying broken work', this entry's own premise) and `.github/` (**CI could be disabled outright**) all shipped ungated; now covered, along with `commands/`, `STEERING.md` and da-paths itself. Pure prose (docs, README) stays OUT deliberately: a gate that fires on a typo trains a reflexive 'clean', and a rubber stamp launders the decision instead of testing it (R53). (b) `bin/da-gate.sh` extracted at ship.sh's 300-line cap; it REFUSES a bare `clean`/`none`/`lgtm`/<24-char note — a clean pass must still say what it EXAMINED. (c) `/companion:ship-it` prose now says ALWAYS run a devil's-advocate pass, scaling DEPTH to risk, replacing 'if the change is consequential' — which asked the model to rate the riskiness of its own work, the least reliable judgment available. **Two silent gate-bypasses found while building this, both in the gate itself:** `grep -Ef` compiles EVERY line of da-paths as a regex, so an unmatched paren in a PROSE COMMENT made grep exit 2, the match come back empty, and the whole R78 gate silently not fire; and the fail-closed check read `PIPESTATUS` from inside a command substitution, where the parent shell never sees it, so an invalid config shipped at exit 0. Comments are now stripped and an unusable config FAILS CLOSED. **Named limit, unchanged and unfixable:** none of this can verify a pass actually HAPPENED — the note is fabricable and no shell check can prove an agent ran. It raises the floor and makes skipping deliberate; that is the ceiling (R28). **A SECOND `--da` pass on this very change found three more fail-OPENs in the gate itself, all reproduced end-to-end merging a `bin/` change to main with no note and nothing on stderr:** the comment-stripping grep's exit status was discarded (an unreadable file or a DIRECTORY in place of da-paths read as 'no critical paths'); **CRLF line endings silently disabled the entire gate** — the likeliest real-world case, since this repo ships no `.gitattributes` and da-paths is a file strangers author; and the new plumbing HARD-BLOCKED a repo with no da-paths at all once `$changed` exceeded the pipe buffer (SIGPIPE -> pipefail -> 141), breaking the documented 'no file ⇒ no gate' promise at 4000 paths. Also removed: `DA_PATHS_FILE`, an undocumented env kill switch that left NO commit trailer — precisely defeating the auditability this entry claims. Also fixed: non-ASCII findings were rejected wholesale (byte-oriented `tr` folded a Japanese note to 0 chars — R1), and a multi-line note could forge a second commit trailer. The first version of the new test RE-IMPLEMENTED the matcher inline and passed with `da-gate.sh match` deleted; it now pipes through the real script.  bats total; 5 mutations across both rounds, all caught. |

| **R69** | 🔒 | **The token budget on injected artifacts is ENFORCED, and steering is two-tier.** (a) `check.sh` byte-caps everything that enters context automatically: the STEERING **injected core** ≤12,288B (+ the marker appearing **exactly once** — zero would silently inject the whole doc, two+ would silently truncate the core), `CLAUDE.md` ≤4,096B, `LESSONS.md` ≤6,144B (grows by design — headroom is deliberate). Caps are ceilings with working room, not reverse-engineered to current content (DA objection, cap raised 10,240→12,288 pre-ship). (b) STEERING.md is split at an `injection stops here` marker: imperative core above (every rule/trigger, canonical), rationale below (on-demand, never injected); `session-start.sh` cuts at the marker, **failing open** to the whole file if absent (best-effort R7 — the check, not the hook, guards the marker). (c) `tq` mutations print a **one-line counts delta** (`📋 ▸n ◻n … → next: #id`); the full report fires only at anchoring boundaries — `done`, `import`, explicit `report`, session-start/compact. | 2026-07-23, owner-picked from a 4-question recommendation menu (provenance `stated`) after a measured audit: the injected STEERING had silently grown to ~5.9k tokens against its own in-code "~2.4k-token" claim (2.5×, unnoticed — proof an advisory budget fails), and the every-op report reprint was linear-in-queue-size spend re-reading what the model just wrote (~3–4k tok over a long drain). **Partially reverses R66's "STEERING trim declined":** R66 feared losing motivated prose; the two-tier split keeps every rule + its trigger in the core and moves only the *why* below the marker, so the behavioral surface survives at ~43% of the injected cost (23.7KB → ≤10KB). Amends **R47** (report cadence: reprint-on-every-change → delta + boundaries; Design-D layout itself unchanged) and the R32 compact-slimming lesson generalized. Composes R7 (fail-open hook), R48 (delta + the injection cut each covered by a behavioral bats case — the cut is un-eyeball-able, exactly R48's sanctioned class), N1/R58 (zero-injection capture untouched). Amends **R8**/**R47** in place (report cadence). Pre-ship DA pass restored three compressed-out rules to the core (backlog-unprompted · owner's-earlier-requests+over-engineering challenge triggers · the R67 decompose-park↔MCP link), keeping the "every rule survives in the core" claim true. |

| **R70** | 🔓 | **`docs` is the boundary-contract producer — sharpened, not replaced.** (a) The scan panel gains an **outside-in boundary lens**: enumerate every input/output at the system edge (CLI surfaces, file formats, exit codes, store schemas, injection shapes) — the archaeology lenses (landmines, invariants, design choices) stay. (b) **Machine-facing interface contracts are flow pages** whose consumer is a program/agent — same `docs/flows/` home, same R66 shape, same R61 gate; NO new doc kind (an `INTERFACES.md` was offered and rejected — the fourth-home/drift objection that already killed `FEATURES.md`, R62-amendment). (c) **Swap-survivability criterion:** a contract item is stack-independent iff its `[E]` test is **black-box at the boundary** (invoke the surface, assert the output — never import internals), so the suite runs unchanged against a reimplementation; `cover` is the mechanizing arm. (d) A quality attribute claims contract tier **only with a stated/confirmed why** — no why → incidental/🔓, never `_quality-bar.md`. **Honesty ceiling (Hyrum's law):** the *complete* observable contract is unenumerable — the claim is "the *agreed* contract survives a swap and everything outside it is *declared* disposable," never "swapping is risk-free"; chasing completeness would rebuild the technical-requirements catalogue R55 forbids. | 2026-07-23, owner-picked from a 4-option menu (provenance `stated`; full-refocus, tests-only, and leave-as-is rejected — full refocus loses the landmine lens incremental change needs). Amends **R41** (scan lenses + routing); composes **R54/R55** (this *is* the pillar routing, outside-in), **R61** (the gate is the survivability enforcement), **R66** (machine shape), **R53** (why-sets-the-lock, now mandatory at contract tier). 🔓 like R41 — a command spec, freely amendable. |

| **R71** | 🔓 | **Latency levers: mechanize the deterministic, background the slow — never localize judgment.** (a) **`bin/ship.sh` rail** — ship-it's deterministic spine becomes one guarded script, two phases: `preflight` (resolve gate → run it → drift backstop → `tq export` → tree/branch summary) and `land -F <msgfile>` (re-run gate → stage → commit → **ff-only** merge to default → push → delete the shipped branch `-d`-only; merged-branch sweep only behind `--prune-all`, default list-only). Claude keeps every judgment step: the case, devil's-advocate, contract-impact naming, flow-page proposal, commit message, history curation; the rail **bails loudly** (distinct exit codes, hand back to Claude) on gate-fail, non-ff, or anything unexpected — never `-D`, never the default branch, never force. (b) **Scan panels run in the background** — `docs` Pass 1 / `advise` critic panels spawn as background sub-agents with a one-line announce; the owner keeps working and only the triage is live. (c) **Boundary honestly drawn:** judgment (advise/docs/redesign/review content) is NOT localizable — a "local heuristic" version is the deleted pre-R24 system (R9 violation); commit-message templates (kills R40) and a local inventory pre-scan (model re-reads anyway) rejected as anti-targets. | 2026-07-23, owner-picked 2-of-4 from a menu (provenance `stated`). **Rejected:** batching AskUserQuestion loops 3-4/turn (cheapest lever, but reverses R38's owner-picked one-at-a-time pacing — owner declined) · prune-only rail (superseded by the full rail pick). **Blast trade-off named:** ship.sh is the biggest-blast `bin/` script (pushes + merges to default); R68 binds hooks, not command tools, but its *spirit* (bounded blast) shapes the bails — the rail executes only what ship-it prose already did by hand, with guards promoted from prose to code. Composes R57 (ship-it stays contract-aware — judgment intact), R35 (prune guardrails now enforced), R60 (export rides the rail), R9/R24 (the localizable/judgment line IS the steering/core split). |

| **R72** | 🔓 | **One-call cross-machine handoff: `ship.sh handoff` — commit WIP off-default + queue export + push.** Sequence: `tq export` (best-effort) → stage all → **refuse staged credential shapes** (it pushes — same backstop as `land`) → commit (`wip/<utc-stamp>` branch when on the default — WIP never lands on default, R34-spirit; in place when already on a feature branch) → `push -u`. The **gate is deliberately NOT required** — a handoff is a checkpoint for another machine, not a ship (a red tree mid-work is normal); the gate fires when the work actually ships (`land`). Clean tree + no queue delta → exit 6 (nothing to hand off); no remote → bail (git IS the transport). Pickup on the other machine: pull → check out the branch → `/companion:resume` (imports the queue, R60). Closes the two honest gaps in the cross-machine story: queue-travels-only-at-ship, and stranded-dirty-tree. | 2026-07-23, owner-picked from a 4-option menu (provenance `stated`; ship-mode-export-only and STEERING-nudge-only not picked — the rail subsumes both audiences; accept-the-gap rejected). Stack/format review same session: bash3.2+jq, md/JSON split, git-as-transport all **reaffirmed** — sync-tool transport rejected (smears un-reviewed state, reintroduces the dual-truth R67 killed; the fix is this ritual, not a second transport). Captures stay local by design (privacy > completeness). Composes R60 (export/import is the queue's vehicle), R63 (identity still doesn't travel — handoff is explicit), R34 (never WIP on default), R71 (same rail file + exit-code contract). |

| **R73** | 🔓 | **Command-review sweep (3-panel + DA) — one enforced outcome, the rest prose.** A ground-up review of all 10 command prompts by three independent lenses (cost · simplicity/coherence · UX) + a devil's-advocate on the ship diff. **Enforced (durable):** `check.sh` now byte-caps each command's `description:` frontmatter at **140B** — it's always-loaded per-session injection (the whole command list rides every session), the exact silently-growing class **R69** capped for STEERING/CLAUDE.md/LESSONS but had missed here; ceiling with working room over the 116B max (`handoff.md`), prevention > detection (N7). **Prose fixes (R28, freely amendable, not individually locked):** autopilot.md now documents the `decisive` arg (functional gap — script/MAP/flows referenced it, the command didn't); the handoff→resume **receiving end** is lit up (resume clears autopilot early, then checks for an un-checked-out waiting handoff branch — `wip/*` from a default-branch handoff *or* the relayed feature-branch name — and points at `/companion:handoff`, killing the pre-R72 manual-export prose; prose-only per R28, `resume.sh` still just clears+imports); docs.md preamble deduped (routing table stated once, honesty machinery kept verbatim — owner-picked moderate cut); advise gained the standard step-0 + a docs-prerequisite pointer; redesign negotiates its interruption budget up front (D2 run-mode pick + exit ramp); shared step-0s defer the R38 pile-review (mechanical-unblock ≠ off-ritual); history-asides stripped from runtime prose. **Set stays at 10** — every merge/split/delete candidate checked, none ripe. | 2026-07-23, three-panel review + DA, owner walked all 14 deltas recommendation-first (provenance `stated`). The DA also caught a real ship defect pre-merge (`land --gate` single-token vs `preflight` varargs) — fixed in 3.16.0 before it landed. Amends **R69** (adds the description cap to the enforced injection budget); composes R28 (prose = judgment, most fixes un-lockable), R41/R59/R72 (the gaps closed), R3 (density). |

| **R74** | 🔒 | **`ship.sh land` ENFORCES a CI watch after the push — a green local gate is not a green CI.** After a successful push, `land` finds the CI run for the pushed commit and blocks until it concludes: **GREEN → exit 0; RED → exit 10** (a distinct "SHIPPED, fix-forward" signal — the commit is already on the default, so the remedy is a forward fix, never an un-ship). **Best-effort + bounded** (the ship already landed, so no watch problem may un-ship): `gh` absent / no run registers / overall `SHIP_CI_TIMEOUT` (default 300s) elapses → reported, exit 0 (unwatched, not failed). Opt out with `SHIP_CI_WATCH=0`; self-disables for repos with no CI (no run appears). Applies to `land` only — `handoff` pushes a throwaway `wip/*`, not a ship. **Amended by R79(b) 2026-07-28:** an abandoned run is exit 12, not 0 — the "timeout ⇒ exit 0" clause here let a red ship report as fine. | 2026-07-23, owner directive ("enforce the CI watch in ship.sh land") after `ship.sh` (added 3.16.0) shipped **red CI twice unnoticed** (3.16.0, 3.17.0): the local shellcheck build silently missed an `SC2015` that CI flagged, and gitleaks/shellcheck **SKIP locally when absent** — so a local `check.sh` PASS was a false green and no one was watching CI. This closes the gap structurally (enforced control-flow in the rail, not an ignorable STEERING reflex — R28/R51). 🔒 because it's a control-flow guarantee with a named failure it prevents (cf. R43/R44/R45 fail-safes). Composes R71 (same rail + exit-code contract), R7 (best-effort/fail-open), R48 (the green/red/opt-out paths each carry a bats case via a stubbed `gh`). LESSONS carries the underlying SKIP-locally / linuxbrew-SC2015 trap. |

| **R75** | 🔓 | **A command's parameters must be visible where the owner invokes it — in *both* the `description` and the `argument-hint`, and `check.sh` holds them to it.** The two serve different moments: the **`description` is what renders while you BROWSE the `/` menu** (and is the only one of the pair the model sees, so it is also what routing keys on), while the **`argument-hint` appears only once the command is already chosen** and is not injected at all. A hint alone therefore never helps you *choose*. Every description leads with its argument shape — `[target] [-- goal: X]`, `[scope]`, or `(no args)` — inside the 140B injection cap (R73); `advise` was trimmed to fit rather than raising the ceiling. Measured cost of carrying the parameters in all ten: **+218B ≈ 54 tokens per session**, which is not a budget worth optimising against. **Description values are QUOTED, and that is load-bearing, not style.** An unquoted YAML scalar beginning `[` opens a flow sequence; the host throws, discards the **entire** frontmatter — description *and* hint — and logs it at debug level, so six commands would have shipped with neither. `check.sh` line-greps the frontmatter and cannot see a parse failure, so it now separately rejects an unquoted value that starts with a YAML indicator or contains `: `. **`check.sh` enforces four things:** the 140B description cap (measured unquoted) · every `$ARGUMENTS`-reading command declares a hint, and no hint promises params the body ignores · the 80-char hint cap (it truncates end-first) · and description ⇄ hint **agreement in both directions**, set-compared on bracket groups. Each `[...]` group contributes *every* alternative, not just its first token — taking only the first hid two thirds of `autopilot`'s surface; a hint with no bracket notation is rejected rather than silently skipping the comparison; an enum-style description with no brackets falls back to substring presence rather than being forced into notation it reads worse in; a bare `[R55]` is treated as a citation, not a parameter. | 2026-07-25, owner-picked from a 2-question menu; **amended 2026-07-26** (owner-picked params-first descriptions + the agreement gate, provenance `stated`), which **reverses this entry's original clause** that parameters belonged in the hint and *not* the description. That original reasoning — that descriptions cost per-session tokens — was directionally right and quantitatively wrong: nobody had measured it, and 54 tokens/session does not justify hiding parameters from the menu you browse. Extends **R73**'s injected-description cap. Argument gaps closed in the original pass and still current: **`redesign [module]`** (one bounded pass — D2 skipped, D0/D1 narrowed to the module and **weaker than the whole-app invariant net by construction**, so D3·R3 re-asserts the check), **`ship-it [pr] [--gate <cmd>]`** (`pr` leaves the rail, so it must name all four forgone `land` guarantees — gate re-run, staged-credential refusal, ff-only merge, R74 CI watch), **`docs [scope]`** (a scoped recap must name what it left out), **`resume [branch]`** (a named branch is authoritative; a missing one skips only the checkout and still runs the pickup). 🔓 — descriptions, thresholds and notation are the most reversible surface here (cf. **R47**, downgraded 🔒→🔓 for over-constraining a display detail). No bats case, matching R73's description cap; every gate assertion was instead exercised against the real gate, and the YAML breakage reproduced with a real parser before and after. **Both devil's-advocate passes earned their keep**: the first caught a broken GFM table, two unnamed `pr` safety losses, a self-contradiction in redesign's scope prose and a `resume` abort footgun; the second caught this entry's YAML ship-blocker, three evasions in the agreement gate, and three wrong figures in this very cell. |

| **R76** | 🔓 | **The status line shows ACCOUNT rate-limit usage, from the payload it already receives.** Claude Code pipes `.rate_limits.{five_hour,seven_day}.{used_percentage,resets_at}` to every `statusLine` command, so the bars cost **no API call, no network, no token** — the same stdin JSON the line already parses (verified in the installed 2.1.220 binary, not just the docs). **Both windows render whenever present** (owner-picked always-on over threshold-only), as a 5-cell bar + integer percent: green <60, yellow 60–84, red ≥85, plus a `↻` reset countdown once a window passes 80% — width is spent on the countdown only when it is actionable. The bar **floors** rather than rounds, so only a genuinely exhausted window shows five filled cells (rounding up painted 87% and 100% identically, the one reading that must stay distinguishable); anything above 0% still lights one cell — including a sub-1% window, which truncates to `0%` in text but keeps one cell lit so “barely used” stays distinguishable from idle. **Honest framing — these are ROLLING windows, not a billing cycle.** The plans meter on 5-hour and 7-day rolling windows; a "% of this month" bar would be fiction, and the docs say so wherever the bars are described. The number is also **account-wide**, so it moves while you work in other repos or other Claude surfaces. **Absence is a first-class path** (R7/R68 best-effort): the field exists only for Claude.ai Pro/Max, only after a session's first API response, and each window can vanish independently — every such path renders **nothing**, never a placeholder or a zero, because a status line must not invent a number it was not given. | 2026-07-25, owner-asked ("show my token use as a bar") then owner-picked always-on-both-windows from a 4-option menu (provenance `stated`; threshold-only, 7d-only, and replace-the-session-⇡⇣-counters all rejected). The ask named a *billing cycle*; corrected to rolling windows before building — the data to back a monthly bar does not exist. **Fixed a latent pre-existing defect in the same change:** the line read its jq output with `IFS=$'\t'`, and tab is IFS *whitespace*, so a run of tabs collapses into one delimiter and every field after an EMPTY one shifts left. Dormant while all five fields were always populated; fatal the moment optional fields arrived (7d's percentage would render under the 5h label). Reader and `join` now use **US (0x1f)**, a non-whitespace separator that preserves empty fields positionally. That swap cost `@tsv`'s implicit `\n\r\t` escaping — a devil's-advocate pass caught it pre-merge: a newline in a path split jq's record, so `read` took only the first line and silently dropped the tokens, both bars and the git segment. Values are now scrubbed of framing characters before joining. Both halves are pinned by regression cases, each mutation-tested. 🔓 like the rest of the status-line surface (cf. **R47**, which downgraded a display detail from 🔒 precisely because layout is the most reversible, taste-driven thing here): thresholds, cell count and placement are all fair game to re-tune. Six bats cases (**R48**) cover both-windows, total absence, one-window absence, clamp + floor, the colour thresholds and the countdown gate; each was mutation-tested — reverting the separator breaks exactly the cases that pin it. Composes R32/R34 (glance surface, plugin-relevance order), R7/R68 (best-effort, never break the host), R9 (no plan/provider allowlist — it renders whatever windows the payload carries). |

| **R77** | 🔒 | **Sweep mode — `/companion:autopilot sweep on`: autopilot also works parks the OWNER MARKED REVERSIBLE.** Opt-in, persisted per-repo, mirrors ship/decisive, shown 🧹. The Stop hook stops treating a `❓`-only queue as finished, so an unattended run applies each eligible park's recorded `rec:`. **Eligibility is a POSITIVE MARKER the parker wrote, never an inference over prose.** A reversible-but-owner's-call park (a taste/design/wording choice — R33) is written `❓ [parked] rev: …`; sweep acts on `rev:` and nothing else. Absence of the marker means irreversible, which is also the safe default for every park written before this existed. Predicate: `❓` ∧ `rev:` anchored to the `[parked]` tag ∧ `rec:` anchored to a `;`/`—`/`-` ∧ not `decompose:` (case-folded). **`⏳` is never eligible in any mode** — it is blocked *on the owner acting in the world*. **This is the second design; the first was unsafe and never shipped.** It inferred eligibility as `❓ ∧ rec: ∧ ¬decompose:` — and under **decisive** mode, which parks *only* the irreversible and writes `rec:` on it, that predicate selected **exactly the set it existed to protect** (a devil's-advocate pass demonstrated it handing a `git push --force` to an unattended run). Inverted selectivity cannot be fixed by tightening a blacklist over free text: reversibility is not in the data unless the writer puts it there. Hence the marker, and hence **`ask-guard.sh` writes it** — plain autopilot marks a taste park `rev:`, decisive is told never to mark its irreversible parks. **Sweep's home is PLAIN autopilot, not decisive** (an earlier draft said "on top of decisive" in five places — wrong: decisive doesn't park the reversible at all, so there would be nothing safe to sweep). **Scope is this session's parks** (`tasks/$sid`), not the cross-session backlog. **Termination is enforced, not assumed.** The no-progress cap **cannot** bound a sweep: closing a swept park advances `DONE`, which resets the stall counter — measured at 25/25 turns blocked against a cap of 8 — and sweep also removes `OPEN==0 → allow`, previously the only model-independent terminator. So a separate counter increments on every turn that continues *solely* because of sweep (no plain work left) and **no completion resets it**; at the same cap the drain yields. Plain productive work stays unbounded (verified >40 turns). | 2026-07-26, owner-picked from a 4-option cost menu, then **re-picked after the first design was shown unsafe** (provenance `stated`). The menu named that this amends 🔒 entries and needs explicit sign-off; picking it IS that sign-off. Rejected: one-shot `/companion:review auto` (no unattended run — the point), widening decisive (loses "decide new things, don't touch my pile"), do-nothing. **Amends R38** — while the flag is on, the parked-pile review is not merely un-gated but *empty by construction* for `rev:` items: the ritual and its triggers survive, the pile it walks does not. **Amends R59** (decisive must not mark its parks `rev:`) and **R33** (a reversible taste choice, normally the owner's even when trivially undoable, may be decided while this flag is on — this is the requirement the mode actually reverses, and the reason it is opt-in and off by default). Composes R26/R36, R65, R28 (the safety decision is now *code*, keyed on a marker, which is why this belongs in a hook at all). **Cost, measured:** +371B injected STEERING (≈93 tokens/session; 1388B headroom) — the mechanics live in the Stop nudge, which costs nothing while the flag is off. 🔒 because it is a control-flow guarantee whose failure is concrete and demonstrated. Five bats cases; **six mechanisms mutation-tested** (`rev:` requirement · `⏳` · `decompose:` · anchored `rec:` · the terminator · the note appearing only when on), each fixture carrying the other markers so no rule masks another — a trap that hid two of these during development, and a vacuous absence-assertion that hid a third. |

| **R78** | 🔒 | **Four controls against this repo's actual failure mode: a green gate certifying broken work.** Across three sessions `./check.sh` passed on a YAML error that silently dropped six commands' entire frontmatter, on a sweep predicate that selected precisely the set it existed to exclude, and on a termination claim that was false. Each was caught by an adversarial pass, not by the suite. **Two of the four are not gateable** — an inverted predicate and an untested claim are judgment, not lint — so the controls split accordingly. **(a) `ship.sh land` refuses (exit 11) a diff touching paths the REPO declares critical, unless `--da <finding>` is given**, recorded as a `Devil-advocate:` commit trailer. Which paths is **not** hardcoded: one extended-regex per line in **`.companion/da-paths`**, absent ⇒ no gate — the first draft baked in the companion's own layout (`plugins/*/bin|lib`, `check.sh`) and would have made any third-party repo with a root `check.sh` **permanently unable to ship**, an R9/R1 violation caught in review. Runs after the gate re-run (a red gate stays exit 4) and before staging (nothing committed on refusal). Detection uses `--no-renames` + `ls-files --others` + `core.quotepath=false`, because a rename reported only its destination, an untracked file was invisible, and non-ASCII paths arrived escaped — three demonstrated bypasses, the worst being **renaming the gate itself away and shipping clean**. `--da` rejects a flag or whitespace-only value; the trailer is written to a COPY so the caller's message file is never mutated and retries cannot duplicate it, and carries no leading blank line so it does not orphan `Co-Authored-By`. **HONEST LIMIT: it cannot verify a pass happened** — it makes skipping deliberate and auditable, not impossible. **(b) `./check.sh --mutate`** applies each mutation declared in `plugins/companion/tests/mutations.txt` and requires the suite to go RED; one that stays green is a **hole** — a test that cannot fail. Three such holes hid real defects in one session. A pattern that no longer matches is itself reported as a hole, so the set cannot rot silently (it has already caught its own staleness twice). Restores via `trap … EXIT INT TERM HUP` and `*.mutbak` is gitignored: it mutates the LIVE tree including `secret-guard.sh`, and without the trap one Ctrl-C left the secret gate disabled and stageable — R7 failing open. **CI-only**, separate job: it costs a full suite run per mutation. 18 declared, all caught (measured by `./check.sh --mutate`); exit 1 on any hole, proved by injection. **(c) A frontmatter lint** on the two keys this gate reads. `check.sh` line-greps frontmatter while the host parses it as YAML and drops the whole block on a throw, so the gate is structurally blind to that failure. **It is a lint on known-bad shapes, NOT a YAML validator** — an earlier whole-block whitelist rejected `allowed-tools:` block/flow sequences and single-quoted scalars, all of which the host accepts, and a gate that breaks valid commands is worse than none. Catches: a leading YAML indicator, an unquoted colon, an unterminated quote, a duplicate key. Does **not** catch every throw (a `? ` indicator, malformed keys it does not read) — named here rather than implied away. **(d) A ledger measurement must name its evidence** — hard units only (bytes / tokens / N-of-N); a rhetorical `~90% of the value` is judgement and must not trip it. **Graded a nudge, not a guarantee:** it fires zero times on the current ledger, and its evidence markers are trivially satisfiable by writing the word, so it cannot prove a number is *right* — only prompt the author to say where it came from, which is the habit that produced two false claims. | 2026-07-26, owner-picked (all four) from a cost menu after a verdict naming the pattern (provenance `stated`). Presented honestly: none costs runtime tokens — gate/CI code, not injected context — so the price is gate runtime (default path 35s → 37s, measured) and maintenance surface. Composes **R28/R51** (this IS the prose-vs-code line: "spawn a devil's-advocate if consequential" was skippable prose, (a) makes it control-flow), **R9/R1** ((a) declares nothing about a stranger's layout), **R48**, **R71** (same rail, one more exit code), **R7/N7**. Amends **R75**'s frontmatter heuristic, replacing it with (c). **Gap narrowed 2026-07-26 (3.24.0), and what remains is named.** (c) and (d) were inline in `check.sh`, which the suite cannot invoke without recursion, so neither had a behavioural case nor a mutation — verified only by unrepeatable manual work, exactly what (b) exists to condemn. Both now live in **`plugins/companion/bin/doc-lint.sh`** (`frontmatter` / `ledger`), which `check.sh` calls and `companion-core.bats` exercises directly: **4 bats cases and 6 mutations** added, all caught (`check.sh` 295 → 264 lines). **The extraction created a new untested failure mode — the CALL:** stubbing out both invocations left the entire suite green while the gate checked nothing. bats still cannot run `check.sh`, so the wiring is held by a **structural guard** (grep the call sites, same shape as the R56·P3 prose guard) plus two mutations — weaker than a behavioural test, and said so rather than counted as closed. 🔒 for (a)-(c): each is a guarantee with a named, demonstrated failure it prevents, and the value is that they cannot be skipped on a tired day. 14 bats cases for (a), every demonstrated bypass pinned; (b) self-verifying at 18/18 (measured). **Four defects the 3.24.0 review raised as nits were fixed in 3.24.1 rather than left unrecorded** — leaving them flagged-but-unwritten was itself the R78 failure mode. (i) A **CRLF or BOM** file made the reader's `$0=="---"` never match, so the frontmatter block came back empty and **every** frontmatter check passed vacuously — fail-open on all of them at once, and the route by which the 3.21.0 blocker returns. Root cause was a **duplicated reader**: `check.sh` kept its own copy of the `awk`, so a fix would have landed in one place. There is now one shared `doc-lint.sh fm`. (ii) The `~` exemption matched `(^|[^~])[0-9]`, which can begin one digit *inside* a number, so `~371B` tripped while `~9B` did not; approximations are now stripped before matching. (iii) Folded/literal block scalars (`>` / `|`) were rejected though the host accepts them — contradicting (c)'s own must-not-break-valid-commands contract. (iv) A missing ledger file exited 0 and `check.sh` printed `ok` for a check it never ran. Each has a bats case and a mutation. |

| **R79** | 🔒 | **A watch that gives up no longer reports success; `--mutate` gains a file filter; the CI watch leaves `ship.sh`.** **(a) `check.sh --mutate [file…]` file filter.** A full sweep is minutes long, so it is CI-only — which means a pattern stale-ed by *the very commit that changed its target* reaches the default branch and fails CI there (3.24.2 did exactly that). The filter runs only the mutations targeting the named files — 8 for `doc-lint.sh` versus 20 full (measured) — so the file you just edited can be checked on its own. **A `land`-time gate was built and then REMOVED before shipping:** a devil's-advocate pass measured it at ~6 minutes for one diff, against the 600s ceiling on a single tool call, so the first real ship would have been killed mid-run. The affordability claim was asserted without ever being converted to seconds. It would also have reversed R78(b)'s 🔒 "CI-only by design" clause and opened a minutes-long window with the live `secret-guard.sh` hook mutated. Running `--mutate <file>` when you touch a mutated file is a **steering** habit, not a gate (R28). **(b) Amends R74 (🔒): a timeout is no longer exit 0.** R74 specified that `gh` absent / no run / timeout all report and exit 0 — "unwatched, not failed". Adding the `mutate` job put CI duration either side of the 300s default, and `land` twice printed *"still running — not failed"* and exited 0; **one of those ships was red on macOS and was found only by checking by hand.** A watch that gave up read exactly like a watch that passed. Now: a run that **exists** and is abandoned exits **12 — SHIPPED but UNWATCHED**; `gh` absent or no run ever registering stays **exit 0**, because having nothing to watch is a different fact from abandoning something. The timeout itself is unchanged — the defect was the reporting, not the duration. **Decomposition:** `ship.sh` was at exactly its 300-line cap, so the watch moved to **`plugins/companion/bin/ci-watch.sh`** (56 lines, callable standalone); `ship.sh` is 291. | 2026-07-28, owner-directed after a verdict naming both holes (provenance `stated`). The R74 amendment was the option named in that recommendation. Composes **R71** (same rail, one more exit code: 12), **R78** (this is the same lesson one level down — (b) built the mutation harness, this makes it reachable before a push rather than after), **R9/R1** (both gates repo-declared, absent ⇒ off), **R7** (best-effort: a watch problem still never un-ships a landed commit). 🔒 because each is a control-flow guarantee with a named, demonstrated failure — a red ship reported as fine, and a stale mutation pattern reaching the default branch. 3 new bats cases for the watch (121 total, measured), and the exit-12 path is mutation-covered (21 declared, measured by `./check.sh --mutate`). **Honest limit:** with no `land`-time gate, a stale mutation pattern still reaches the default branch and surfaces in CI — exit 12 is what makes that visible instead of silent. |

| **R80** | 🔓 | **The recommendation posture is stated as a default, and the closing verdict is unconditional — plus the status line stops shouting zero.** Owner-reported: recommendation-first options and the brutal-honest verdict "stopped working for most requests unless I specifically ask". **Diagnosed three causes; only two were real, and the first claim was wrong.** (i) I asserted the post-compaction re-anchor dropped the posture entirely — **false**: `session-start.sh` already re-stated the recommendation-first half inline (R30·d2 excludes the *core prose*, not the posture). I read the comment and a grep instead of the message string, and a decision was taken partly on that basis. The genuine, narrower gap was that the **unconditional closing verdict** was absent from that message while the options half was present; ~40B added, and the comment corrected to say what is actually carried. (ii) **Real:** STEERING had TWO `Moves:` blocks, and the *lead* one opened with the escape hatch — "is this decision-shaped? … but don't menu-ify a trivial ask" — undercutting the stronger later block ("never a flat answer", R5/R49) that says what the owner wanted. The lead now **defaults to options** (a flat answer to a decision-shaped ask needs a reason) and splits the verdict out as its own unconditional item, flagged as the half that decays first. Core 11142B/12288B (measured). (iii) **Real, and mine:** the verdict clause was already unconditional in the text and I was following it perhaps half the time. No mechanism fixes that; naming it is the honest part. **Named limit:** nothing here *enforces* the posture — `AskUserQuestion` is a tool the model chooses to call, so all three changes move the odds, not the guarantee. That is the advisory-vs-enforced weakness R28 describes, and the owner's own memory records it as the suite's core weakness. Rejected as the fix: a UserPromptSubmit hook injecting the posture on every prompt — it would cost ~38-95 tokens per prompt and is precisely the shape **R28/R51** forbids ("don't add advisory prose as a hook"), the ~15-hook advisory system R24 deleted. **Also (unrelated, same ship):** `📋` on the status line rendered permanently, even at 0, while every other indicator there is shown only when relevant (`🛡✗` only when the gate is off, `❓`/`⏳` only when non-zero, `↑↓` only when diverged). A drained queue now renders no queue section and no divider; one open task brings it straight back. Prompted by the owner asking whether the queue section was redundant with the CLI — it is **not**: the CLI prints the queue only at mutations and anchoring boundaries (R69), so between those the status line is the sole always-visible view, and it is the surface that costs **zero** injected tokens. Removing it would push toward running `tq report` more, which costs tokens the status line does not. | 2026-07-29, owner-picked from a 4-option menu after reporting the decay (provenance `stated`); the status-line trim owner-picked from a 3-option menu. Composes **R5/R49** (the posture itself), **R30·d2/R32** (what a compaction re-anchor carries — narrowed, not reversed), **R69** (the injected budget), **R34** (shown-only-when-relevant, which `📋` now finally obeys), **R28** (why the enforcing option was declined). 🔓 — wording, thresholds and which indicators show are the most re-tunable surface here (cf. **R47**). 2 bats cases, 2 mutations, all caught (measured). |
| **R81** | 🔒 | **A hook's cost stays BOUNDED as the project and the task store grow, and the bound is MEASURED in CI, not asserted.** Owner-reported symptom: the machine "occasionally stalls and runs very hot". Measured cause, on the owner's real store (1136 task files / 4.9MB across 42 session dirs): `session-start.sh` read **every task file ever written**, one `jq` spawn per file — **2085ms** at current size, **16108ms** at 8× (358 spawns), on a path that fires at **every session start AND every compaction**. That is the stall. Fixed by R81·a below; the *rule* exists because the scan shipped anyway under 80 numbered requirements and a `check.sh` that never timed anything — an unmeasured budget is not a budget (the same lesson **R69** learned for injected bytes, now applied to hook *time*). **(a)** `companion_open_tasks` batches to ONE `jq` over all matching files (358→4 spawns, 56.7× faster, byte-identical output; 2 mutations caught). **(b) The doctrine:** a hook may read the project — the ban is on *unbounded* work, not on the project. `secret-guard` scanning edit content is fine (bounded by the payload); globbing every session dir ever created is not. Concretely: no hook may cost enough to stall the session on a realistically-large store. **Corrected 2026-07-29 after a devil's-advocate pass:** this entry first claimed hooks are *O(1) in store size* and made a scaling RATIO the hard gate. Both were wrong. `session-start` legitimately READS the store and is ~linear in it, and the ratio verdict turned out to be a function of the gate's arbitrary baseline — passing at 200 files (1.85x), FAILING at 800 (3.72x). A gate you can flip by choosing a baseline measures the baseline, not the code. The hard gate is now the ABSOLUTE cost on the oversized store (sufficient alone: the original regression measured 8585ms against a 1500ms cap); the ratio is REPORTED and fails only on the plainly pathological (>6x). **(c) The gate:** each hook declares an ms ceiling; `check.sh` builds a synthetic **oversized** store, times each hook against it, and **FAILS** on breach — so the next unbounded scan cannot ship. **Rejected as the fix (owner-considered, 3-option menu):** "hooks may only see the prompt, never the project" — it reads as the safe rule but *deletes cross-session resume* (session-start's entire purpose), strips branch/dirty from the status line, and **misses the actual heat source**, which is `statusline.sh` — not a hook, so the rule would leave it untouched. Also rejected for now: an index file `tq` maintains on write (genuinely O(1), but adds index-drift as a new failure mode). **Named limit:** this bounds *per-invocation* cost; it does NOT bound invocation *frequency* — the always-on status line (~212 CPU-s/hour/window, sys-dominated fork/exec churn) is governed separately, and autopilot deliberately removes the idle state altogether. | 2026-07-29, owner-picked from a 3-option doctrine menu (provenance `stated`) after a measured hook audit; every number here is measured, not projected. Composes **R28/R51** (only block/inject/control-flow lives in code — this bounds what that code may *cost*), **R68** (best-effort hooks: a hook must not break its trigger; now also must not stall it), **R69** (the same measure-don't-assert move, for time instead of bytes), **R7** (fail-open). Supersedes the informal "hooks stay lean" prose in CLAUDE.md, which had no teeth. 🔒 — the ceiling *values* are tunable, the measure-it-in-CI requirement is not. **Measured outcomes of the audit this entry came from (2026-07-29):** `session-start` 2085ms→97ms (358→4 spawns, byte-identical output); the status line 225→33 CPU-s/hour/window (**85.5% cut**: git segment TTL-cached + `refreshInterval` 3→10, and `/companion:setup` now REPAIRS a stale interval); `secret-guard` 57ms→25ms and 11→5 spawns on the path that blocks EVERY edit (lazy per-repo-flag read; 3 jq→1; proved byte-identical across a 25-case differential incl. all three content fields, multibyte, 60KB payloads and every fail-safe path, plus 2 mutations); `capture.sh` deleted outright (R58·a retired — 64ms/13 spawns per prompt for a store with zero readers); `tq prune` added to keep the store bounded rather than merely cheap — a CLI command, deliberately NOT a hook, because R68's never-delete rule is right. **Declined on measurement:** ship-mode's double tree-walk (47ms status + 41ms `git add -A` on a 17k-file repo) — the status gate is load-bearing against a stray `autopilot/*` branch every clean turn, and both removals trade 41ms/turn for either a leaked staged index or silently omitting work from a checkpoint. Parked with `rec:` rather than done — and REVIEWED with the owner 2026-07-29, closed WONTFIX. **Autopilot RUN bounds (same audit, owner-picked from a 4-option menu):** the plugin's own share of an 8-hour drain is ~5.4 min CPU — **~1% of the run** (the other 99% is inference plus the tool calls the model makes), so making the plugin faster CANNOT fix overnight heat; the only lever is stopping sooner. The no-progress cap could not do it: `stall` resets on every completion, so a *productive* drain had no plugin-side terminator and ran until the queue emptied or the account rate limit intervened. Added two bounds, whichever fires first — `CLAUDE_COMPANION_AUTOPILOT_HOURS` (6) and `CLAUDE_COMPANION_AUTOPILOT_TURNS` (400), `0` disables either. **Defaults are ON, deviating from the off-by-default the menu described** — flagged to the owner: an opt-in bound does not protect the unattended overnight run that motivated it, and this matches the existing `AUTOPILOT_MAX` stall cap, which has always defaulted on. The clock includes suspend (a machine that sleeps then wakes yields rather than resuming a burn); the turn cap is suspend-immune, which is why both exist. Backward-compatible with the 3-field continue-file. 3 bats cases, 2 mutations, all caught. **The `--da` pass before shipping caught FIVE real defects that `check.sh` was fully green through — the single most valuable half-hour of this work, and evidence for R78's whole premise (a green gate certifying broken work):** (i) the batched `companion_open_tasks` ABORTED at the first unparseable file, so ONE corrupt task file silently rendered 7 open tasks as **0** — a total backlog wipe on the crash-resume path, exactly the failure `tq export` had already learned to avoid; fixed with a per-file fallback that runs only once something is already broken. (ii) `tq prune` read jq's STDOUT and not its EXIT STATUS, so an unreadable file (permissions, I/O error, flaky SD card) counted as *zero open tasks* and DELETED a store holding a ❓ parked decision. (iii) `tq prune`'s `rm -rf` followed a SYMLINKED session dir and emptied the target outside the store, reporting success. (iv) `secret-guard` FAILED OPEN on non-string content — jq's `+` throws on an array, so NotebookEdit's `new_source: [...]` let a real credential through, breaking R43's no-bypass invariant; and (v) it FALSE-BLOCKED an empty edit by scanning the file PATH as content. All five reproduced by hand, fixed, and pinned with 6 bats cases + 3 mutations. The R81 evidence line above overclaimed a *byte-identical 25-case differential* for secret-guard: that differential never exercised non-string or empty content, which is precisely where it broke — the differential is now 26 cases including arrays, objects, numbers, null and leading newlines. Also fixed: the gate measured NOTHING under a comma-decimal locale (`TIMEFORMAT` renders through `LC_NUMERIC`; every sample parsed to 0 and reported green) — `LC_ALL=C` now pinned by a test. |

**Not in the ledger, by design:** AGENTS.md (maintainer-guide prose) and owner **memory** (personal, cross-session, lives outside the repo).
