# REQUIREMENTS — the locked / challengeable ledger

The **single source of truth** for this project's durable requirements. CLAUDE.md
invariants and [docs/ROADMAP.md](./ROADMAP.md) reference entries here **by ID** instead
of restating them — this file is where a requirement's *status* lives. *(The 2026-07-11
ground-up rebuild collapsed the four plugins into one `companion`; entries touched by it
are annotated below — see **R24**.)*

*Organized **chronologically by R-ID** (this ledger's native axis — it's decision history).
It sits at the centre of the shared spine: the contract pillars (`docs/UX.md`, `NFR.md`,
`INVARIANTS.md`) reference **this** file **by ID**, not the reverse — so there are no per-row UX
links here (that direction would duplicate the pillars, against R2 one-canonical-home).*

**Status vocabulary**

- 🔒 **locked** — *changing* it needs explicit owner sign-off; but *challenging* it is
  **invited** (R52·3) — the recommendation loop should actively surface challenges to locked
  entries rather than treat them as settled. Silence-by-deference is itself a miss.
- 🔓 **open** — up for challenge; the loop may propose changing these on its own signal.
- ⚰️ **retired** — no longer in force; kept for history with a pointer to what replaced it.

**The rule (R5):** every recommendation and every task-conversion **cites the R-IDs it
touches and names any it would change** — a requirement is never retired silently, in
either direction.

| ID | Status | Requirement | Rationale · Retires · Supersedes |
|---|---|---|---|
| **R1** | 🔒 | This is a **wide-audience product**, not a personal rig. Generic (language/framework-agnostic) rules are load-bearing; the enforceable content lives in **R9**. | 2026-07-11. Resolves the "personal tool vs product" straddle in favour of *product*. *(Rebuild R24: "independent **per-plugin** installability" is now moot — one `companion` plugin.)* **Reworded 2026-07-17:** dropped "README discoverability is load-bearing" — it contradicts the owner's "README is never loaded" and the Claude-facing / CLI-only doctrine (nobody browses these docs by hand). The product + generic-rules framing stands. |
| **R2** | 🔒 | A **single Requirements Ledger** (this file) is the source of truth for durable requirements; CLAUDE.md and ROADMAP reference it by ID. | 2026-07-11. Fixes the old five-surface scatter (achieved: charter's decisions surface was deleted in the rebuild; CLAUDE.md + ROADMAP now point here). |
| **R3** | 🔓 | Token efficiency is a **core value**, but is no longer enforced as a per-hook character-budget NFR. | 2026-07-11, **reshaped by the rebuild (R24)**: `tests/token-budget.bats` and the per-hook ratchets were **retired** — that apparatus defended a cost the steering-doc-read-once-per-session model doesn't incur, and it drove prose into cryptic, unmaintainable anchors. Efficiency now means "the steering doc stays lean," not a CI char-count. **Clarified 2026-07-17:** the 🔒→🔓 downgrade scopes to the **enforcement mechanism** (the per-hook char-budget NFR is retired) — **not** the value. Token efficiency remains the **core lens** (owner: "it's THE driving principle"; R50 calls the steering injection "the one output with real token cost"); only its CI ratchet was dropped. |
| **R4** | ⚰️ | ~~De-duplicate cross-plugin code via a build step → four installable plugins~~ | Retired 2026-07-11 by the rebuild (**R24**): there are no longer *four* plugins to de-duplicate across — one `companion` plugin means no cross-plugin duplication and **no build step**. The problem R4 solved was dissolved, not solved. |
| **R5** | 🔒 | Recommendations default to **brutal honesty + multiple-choice**, and **must name the R-IDs / architecture a recommendation touches or would change.** | 2026-07-11. Promotes the 2026-07-01 "a requirement conflict is a surfaced trade-off, names the requirement it would retire" clause from a buried rule to the default interaction. Extends it from *decisions* to *this ledger*. **Canonical home for the recommendation / brutal-honesty contract (2026-07-17, per R2 one-canonical-home):** **R17** (critique posture), **R49** (its decision-shaped *park-the-full-payload* refinement), and **R51·3 / R52·1** (the default-interaction-shape + nudge extension) all **operationalize R5 and carry no independent authority** — they cite R5, they don't restate it. |

## Durable requirements & decisions (migrated 2026-07-11, per R2)

The hard invariants (from CLAUDE.md) and durable design decisions (from ROADMAP) now live
here as the canonical status record. CLAUDE.md keeps the invariant *statements* (it's the
only auto-loaded doc — R3), tagged with these R-IDs; ROADMAP's decision sections became
pointers here. Blow-by-blow history stays in `git log`.

| ID | Status | Requirement | Rationale · Retires · Supersedes |
|---|---|---|---|
| **R6** | 🔓 | The plugin is self-contained (Claude Code installs a plugin's subdir alone, so it can only use files under its own root). | Old CLAUDE.md invariant #1. **Platform fact, not a defended decision** — the loader *enforces* this; no design choice here can violate it, so 🔒 was the wrong tier (downgraded 🔓 2026-07-17). Becomes a **live** concern only if **R52**'s `tq`-standalone extraction ships (then per-package self-containment matters again). *(Rebuild R24: one plugin, so there was never cross-plugin sharing to forbid.)* |
| **R7** | 🔒 | **Hooks are best-effort and must never break the triggering action *through error or failure*** (`set -uo pipefail`, swallow errors, exit 0 when silent). The **one intentional exception** is the R19/R43 secret gate's `exit 2` — a sanctioned block, not a failure. | CLAUDE.md invariant #2. A companion that breaks the user's action *by malfunctioning* is worse than one that stays silent. **Reworded 2026-07-17:** the old absolute "never break the action" literally contradicted the secret gate (the one hook designed to block a Write); the real rule is never break it *incidentally*. |
| **R8** | 🔒 | **`tq` is THE task queue** — the companion owns its own task store (`~/.claude/companion/tasks`, not `~/.claude/tasks`) and does **not** use Claude Code's native task tools. `tq add/doing/note/done/list/report` is the whole queue; the report fires on every state change; each session dir is root-stamped so resume scopes to a repo with no native transcript. | 2026-07-11 (owner directive: *actively avoid native tasks*). Was a *fallback* for gated models; now the sole mechanism. Reverses the task-queue half of **R10** — see there. |
| **R9** | 🔒 | **Rules stay generic (wide audience)** — no hardcoded language/framework/ecosystem allowlists. Delegate *recognition* to the model; hardcode only *invocation* a hook can't avoid (prefer the project's own tool); detect *structure* generically. | CLAUDE.md invariant #5; the concrete mechanism behind **R1**. An allowlist rots and biases the suite to one audience. |
| **R10** | 🔒 | **Native-first for hooks, statusLine, AskUserQuestion, and permissions/`auto`** — use Claude Code's native mechanism there; hooks earn their keep only where they *execute* on an event or read state a session can't see. **Exception: NOT the task queue** — the companion owns its queue (**R8**). | ROADMAP durable decision. 2026-07-11: the "native task list" clause was **retired** at owner directive (actively avoid native tasks — they're gated off on the newest models and the queue should be self-owned and stable). Native-first still holds everywhere else. **Note (2026-07-17, R51):** "native-first for **AskUserQuestion** / statusLine" is now partly **platform-forced** — those are among the only three plugin-addressable surfaces, so native isn't a live *choice* there. The one genuinely contestable native-vs-own call left is the queue (**R8**). |
| **R11** | 🔓 | **Run in auto** — recommended owner `settings.json`: `permissions.defaultMode: "auto"` + a `deny` set + an `ask` set (force-push, `reset --hard`). | ROADMAP durable decision — the safe-autonomy posture the owner asked for. **Downgraded 🔓 2026-07-17:** this is **owner-environment config the plugin cannot enforce** (`/companion:setup` wires the status line, not `permissions`), so it's a recommended posture, not a plugin invariant. The binding part is the **reversible-only safety model** autopilot composes with (R26/R34); the deny/ask examples should be refreshed to ones that actually bite (`rm -rf /` is refused without `--no-preserve-root` anyway). |
| **R12** | 🔓 | **Proportionality over maximalism** — every practice scaled to the change's complexity/risk. | ROADMAP durable decision. **Downgraded 🔓 2026-07-17:** a **steering value** (lives verbatim in STEERING's clean-code clause — blast-radius, "~300 lines is a seam smell," YAGNI) and overlaps **R13**; by **R28** a judgment-value belongs in STEERING, not as a plugin-enforced 🔒 constraint. Kept as the ledger anchor for the value; not enforced. |
| **R13** | 🔒 | **Verification + simplicity over methodology labels** — encode the essence (SOLID/DDD/YAGNI, boring & reversible) as concrete generation-time rules (no-seam + deletion-test; unit-cohesion + complexity-altitude), **not** as methodology checkers (a "SOLID checker" isn't mechanically viable). | ROADMAP durable decision. |
| **R14** | 🔒 | **Non-technical-owner posture** — autonomy on the reversible, plain-language consent on the consequential (the line is reversibility + cost + data-safety); verification must be **observable** (demo it working, not read code). | ROADMAP durable decision. |
| **R15** | 🔓 | **Subtractive force + quiet injection** — inject the working agreement **once per session** (SessionStart), then on compaction re-anchor with queue + LESSONS + pointer (not the full doc, R30·d2/R32·3); *state* (carry-over, drift) is never suppressed, only policy prose. | ROADMAP durable decision; the mechanism behind **R3**'s per-session leanness. **Reworded + downgraded 🔓 2026-07-17:** the old text ("bootstrap-once via a `claude-companion` marker… re-anchor in **one line**") was pre-rebuild framing — the current mechanism is `session-start.sh` injecting STEERING once and re-anchoring at ~2.4k tokens on compaction, not one line. Serves the (now 🔓) R3 value; 🔓 to match. |
| **R16** | 🔓 | **Clean ≠ correct** — weigh new work against the recorded direction (this ledger + decisions + roadmap) before it lands, at both intent-time and outcome-time. | ROADMAP durable decision. **Downgraded 🔓 2026-07-17:** unverifiable steering, restated verbatim in STEERING ("weigh against recorded direction… at both intent-time and before done") and overlapping R5/R17; by **R28** a judgment clause lives in STEERING, not as an enforced 🔒. Kept as its ledger anchor. |
| **R17** | 🔓 | **Critique posture: selective, substantive-gated, bidirectional, self-challengeable** — EVALUATE before executing; challenge both recorded constraints *and* the owner's own accumulated bias; object only on real signal. The mandate itself stays challengeable. | ROADMAP durable decision (2026-06-19). **Folded into R5 2026-07-17:** R5 is the **canonical** contract home; R17's payload (bidirectional, self-challengeable, object-on-signal) is carried more sharply by R49, R52·3, and STEERING's always-on honest-verdict clause (R51·3). Kept as the origin anchor, downgraded 🔓 — **operationalizes R5, no independent authority.** |
| **R18** | ⚰️ | ~~**Decided against a charter doc-inventory state file** — chose the CI drift-guard instead.~~ | ROADMAP "decided against" (2026-06-01). **Retired 2026-07-17:** obsolete on every axis — "charter" was deleted by **R24**, the **CI drift-guard it endorsed was also deleted by R24** ("the drift-guard that policed self-imposed duplication — none of it verifiable"), and its forward-pointer was to **R4** (itself ⚰️). A 🔒 entry endorsing a deleted mechanism for a deleted subsystem cannot be in force. The underlying question, if it ever returns, belongs to R24/R52. |
| **R19** | 🔒 | **Decided against a hard plugin-owned destructive-action gate** — gating is **native** (`permissions.deny`/`ask` + `auto`). Narrow exception: the PreToolUse **secret floor** (`secret-guard.sh`) blocks a write, because native permissions scan commands/style but nothing scans file *content* for committed credentials, and a leaked key is irreversible. | ROADMAP "decided against" (2026-06-16; secret-floor exception 2026-06-21). **Reworded 2026-07-17:** dropped the dead 4-plugin name "tidy" → `secret-guard.sh`. The live surface of this exception is now carried by **R43** (all content tools), **R50** (feature flag), **R32·6** (generic-warn vs anchored-block); R19 remains the origin anchor. |
| **R20** | 🔒 | **Decided against native plan mode** for the present-before-work step — the task-queue's interpret→present→approve loop is used instead (plan mode is read-only + all-or-nothing per session; the owner wants to run in auto and review only the queue interpretation). | ROADMAP "decided against" (2026-06-16). |
| **R21** | 🔒 | **Decided against one single CLAUDE.md as the only doc** — use a few lean Claude-context files (CLAUDE.md + map + this ledger + LESSONS/GLOSSARY on demand). | ROADMAP "decided against" (2026-06-16). **Reworded 2026-07-17:** the recorded *rationale* was dead — "charter's separate-file detection" no longer exists (R24 deleted charter) and "per-plugin CONTRACTs" was 4-plugin-era (one plugin, one STEERING.md now). The *decision* still holds for a **current** reason: CLAUDE.md is the only auto-loaded doc (**R3**), so keep it lean and split the rest by responsibility (**R24**), loaded on demand. |
| **R22** | ⚰️ | ~~Decided against consolidating the 4 plugins into 1~~ | Fully reversed 2026-07-11 by the rebuild (**R24**): consolidation to **one** plugin actually happened — going further than R4's "shared source, four installables." The 2026-05-31/-06-16 "delete first, then judge; revisit only if it bites" *did* bite. |
| **R23** | 🔒 | **Anti-rework floors** are bounded, disable-able, **detect-not-decide** gates that supply facts for the model to judge. | ROADMAP "prevention taxonomy". *(Rebuild R24: the only floor kept as **enforced code** is the PreToolUse **secret gate** — it blocks. The others (alignment, intent→outcome, regression) were **advisory** Stop-hook prose that the model could skip; they now live as guidance in `STEERING.md`, not as hooks — the honest home for un-enforceable steering.)* *(**R28** 2026-07-12 **vindicates** this: R27's brief attempt to deliver **intent→outcome** as a PostToolUse reminder hook was retired — it's pure STEERING prose again, exactly as this entry holds.)* |
| **R24** | 🔒 | **Architecture: one steering document + a tiny enforced core.** All steering (queue discipline, critique/recommendation posture, clean-code standards, autopilot) lives in **one** file (`plugins/companion/STEERING.md`), put in context once per session. Only behavior that must **block, inject, or guarantee control-flow** is code: the secret gate (block), session-start injection + cross-session resume (inject — session-boundary I/O the model can't self-perform), autopilot (control-flow), and the `tq` queue. | 2026-07-11 ground-up rebuild (owner-committed). The old system was prompt-injection middleware whose ~95% advisory prose was scattered across ~15 hooks and four plugins, defended by a token-budget NFR and a drift-guard that policed self-imposed duplication — none of it verifiable. Collapsed ~12,500 lines → a few hundred. Separates the reliable (enforced) from the ignorable (steering) so each is honest about what it is. Supersedes/reshapes **R1, R3, R4, R6, R22, R23**. **Amended 2026-07-17 by R52:** the *binding* content of R24 is the **discipline** — tiny enforced core, one steering doc, reliable-vs-ignorable kept separate, no seam-sprawl. The **single-plugin *packaging*** is **no longer a requirement** (downgraded to current-state fact): a deliberate future extraction (e.g. shipping `tq` standalone) is a fair, un-blocked option and does **not** reverse R24 — provided it honors the anti-sprawl principle. What R24 forbids is *unmotivated* fragmentation, not packaging per se. |

| **R25** | ⚰️ | ~~**Clean-as-you-touch is a real hook — format-only** (reshaped by **R28**). A PostToolUse[Write\|Edit] pass (`bin/touch.sh`) formats the edited file with the project's own formatter — the one genuine *execution* worth a hook.~~ | 2026-07-11 (owner directive, after the rebuild); **reshaped 2026-07-12 by R28**; **retired 2026-07-17 by R51.** Originally the hook also *surfaced blast radius and flagged over-budget size*; R28 moved those nudges to STEERING and left `touch.sh` doing only the format. **R51 then retired `touch.sh` entirely** (owner directive to move away from advisory hooks): formatting is a low-stakes, reversible convenience — a *nudge*, not an irreversible guarantee — so it belongs in STEERING like every other judgment call, leaving the enforced core with **no *execute* hook at all** (only block / inject / control-flow). Whole-project cleanliness sweeps remain in `/companion:advise`. |

| **R26** | 🔒 | **Autopilot is enforced + persisted, not just steering.** A per-repo flag (`/companion:autopilot on\|off`, `bin/autopilot.sh`) survives restarts; while on, the **Stop hook** (`stop-autopilot.sh`) auto-continues the queue until only ❓/⏳ remain (no-progress capped) and the **ask-guard** (`ask-guard.sh`, PreToolUse[AskUserQuestion]) blocks asking. The status line shows ✈️. The **design-preview** was restored to STEERING as the wireframe convention (`╔═╗`/`▒`/`█`), steering-only — no gate. *(R27 briefly made it an enforced block; **R28** 2026-07-12 retired that — design-preview is steering-only again, as this entry always had it.)* | 2026-07-11 (owner directive). Two of the five capabilities the owner checked were advisory-only after the rebuild; autopilot was pulled back to **enforced** (a second conscious partial-reversal of R24, after R25), design-review to a **richer steering** clause. The enforcement machinery (a Stop hook + a PreToolUse deny + a persisted flag) is deliberately re-added — the owner values the guarantee. |

| **R27** | ⚰️ | ~~Two enforced work-gates (design-preview + return-review) + one advisory intent→outcome reminder, as PreToolUse/PostToolUse/UserPromptSubmit hooks.~~ | Retired 2026-07-12 by **R28** (owner directive), one day after it landed. The gates enforce *workflow judgment* and the reminder is a *nudge* — both belong in STEERING under the R28 principle, not in hooks. Deleted `work-guard.sh`, `prompt.sh`, `intent-note.sh`; stripped the gate helpers (`has_parked`, `*_flag`, `looks_visual`, `decisions_path`) from `lib/companion.sh`; removed the 🎨/🔒 status-line icons; reverted `ask-guard.sh` to autopilot-only. The three clauses live on in STEERING as prose (they always did — the gates were redundant enforcement). A worked example of the thrash R28 exists to stop. |

| **R28** | 🔒 | **The hook/steering boundary is decided by a component's *nature*, not case-by-case.** Enforced **code** ONLY for what must **block** (the secret gate's `exit 2`), **guarantee control-flow** (autopilot), or perform **session-boundary I/O the model can't do itself** (session-start injection + resume). Everything that is **judgment** (wireframe-first, weigh-against-recorded-direction, present-parked-first) or an **advisory nudge** (blast-radius, size, outcome-recap, formatting) is **STEERING** — prose the model reads once per session and applies. The deciding rule: *block / control-flow / session-boundary-I/O → hook; judgment or nudge → steering.* | 2026-07-12 (owner directive). Re-affirms the **R24** line after the R25/R27 drift bolted hooks onto nudges and workflow-judgment (and after a session of thrash — one gate flipped block→advisory→steering in an afternoon). **Retires R27** (design/return gates + intent reminder → STEERING), **reshapes R25** (touch is format-only; blast/size → STEERING), and **vindicates R23** (intent→outcome was always un-enforceable steering). **R26 autopilot stays enforced** — a control-flow guarantee against the model's own helpfulness bias is neither a nudge nor mere judgment. The rule exists to end the re-litigation, not to be re-argued per feature. **Reworded 2026-07-17 (R51/R52 sweep):** the old text named "**execute** (the formatter pass)" as a category — but **R51 deleted the formatter pass**, leaving *execute* with no members, so the stale example was actively misleading a reader of THE dividing rule. Recast as **block / control-flow / session-boundary-I/O**: "inject" is **not enforcement** (it delivers the *ignorable* steering layer) — its honest justification is session-boundary I/O the model can't self-perform, not that it "guarantees" anything. The **principle is unchanged and stays locked**; only the category scheme + dead example were fixed. |

| **R29** | 🔒 | **`/companion:advise` — a repeatable brutal-honesty critique ritual.** A command (not a hook — pure judgment/workflow per **R28**) that takes a target (file / subsystem / decision / free-text topic; default: the whole project), spawns an **independent critic panel** (distinct lenses — correctness · simplicity · UX · cost · steelman-then-attack) so the critique comes from contexts that *didn't build the thing*, synthesizes deltas, and presents each as a **recommendation-first `AskUserQuestion`, one at a time**, then closes the loop into `tq` + an offered ledger entry. Every critic is licensed to conclude "already right — no change." | 2026-07-12 (owner directive). Operationalizes the **R5** brutal-honest recommendation posture and the **R17** critique mandate as an on-demand ritual, and applies the **R28** independence insight (an outside context can't rubber-stamp what it didn't build). A manufactured delta would be the fake pushback R5/R17 forbid, so "no change" is an allowed, encouraged verdict. Owner-present by design — the ask-guard blocks it under autopilot. |

| **R30** | 🔓 | **Claude-first refinements — 9 owner-decided deltas (all built, 1.7.0–1.9.0).** A ground-up-redesign pass (goal: "designed primarily for the agent that runs it, similar UX + code quality") produced 9 selected changes: **(1)** `tq` tasks gain a `done-when` acceptance field; **(2)** re-inject the queue on context compaction (`PreCompact` + `SessionStart[compact]`); **(3)** STEERING → hybrid *checklist-header + prose*; **(4)** formatter = detect the project's own command (pre-commit / package.json / Make), fall back to the per-ext table; **(5)** `/companion:audit` → multi-agent workflow; **(6)** challenge posture → a required *risk / what-changes / R-IDs* slot in `ship-it` **plus** a devil's-advocate sub-agent for consequential changes; **(7)** a curated, model-maintained `LESSONS.md` injected at SessionStart; **(8)** a strict CI lane (bash-3.2 · `set -u` · no formatters · no git identity) + a hook-fuzz test; **(9)** the status-line beacon animates *on activity only* (static, no timer, when idle). | 2026-07-12 (owner directive, decided via a one-at-a-time recommendation menu). Recorded per **R5**; **build pending and sequenced in batches** — several touch hooks/schema/CI, so not one mega-change. Deltas 3/6/7 sharpen **R5/R17/R28**; 4 sharpens **R9**; 8 is the CI-robustness lesson from the 1.4.x macOS/jq bugs. Each delta ships with its own tests + docs; this entry tracks the set until built, then splits into per-delta entries or updates in place. 🔓 while unbuilt — fair to re-sequence or drop a delta. *(**All 9 built + shipped** across 1.7.0–1.9.0: Batch 1 — d7 `LESSONS.md` · d9 activity-only beacon · d4 formatter-detect; Batch 2 — d1 `done-when` · d3 STEERING checklists · d8 CI fuzz + strict lane; Batch 3 — d2 compaction re-anchor · d6 challenge slot + devil's-advocate · d5 audit sub-agent panel. Now shipped.)* **Demoted to 🔓 as a build-tracker (2026-07-17):** R30 promised to "split into per-delta entries" once built and never did — it is a 9-item **changelog frozen at 🔒**, and several sub-deltas were since reversed/refined elsewhere (d2's PreCompact half + d4 + d5 + d9 by **R32**; d1's render half by **R47**). Its live survivors (d3 STEERING format, d6 challenge slot, d7 LESSONS, d8 CI lane) are folded into STEERING/R48 — treat those as authoritative, not this bundle. Kept 🔓 for history, non-binding. |
| **R31** | 🔒 | **Playtests are autopilot-conditional.** Autopilot **ON** → never raise a playtest (the owner is away); capture the need as a `⏳ [blocked] playtest: <what>` task, silent, resurfaced on return with the parked pile. Autopilot **OFF** → offer a quick playtest when the change has a human-observable surface (UI / CLI flow / visible behavior). | 2026-07-12 (owner requirement). **Reshapes** the prior *"a human playtest is the one thing never parked — note it, keep going"* clause: under autopilot it now becomes a `⏳` (silent, resurfaces) instead of a mid-flight "playtest pending" note. STEERING, not a hook — there's no tool call to gate model output, so per **R28** this is prose, not machinery. |

| **R32** | 🔓 | **`/companion:advise` self-critique — 9 corrections (all built, 2.0.0).** An independent 4-lens critic panel (simplicity · correctness · UX · cost), run on the whole plugin via the R29 `advise` flow, produced 9 owner-selected fixes: **(1)** statusline `read` → `IFS=$'\t'` (**confirmed bug**: a space in the model name/cwd corrupted the parse — task counts read 0, branch broke; untested); **(2)** `touch.sh` drops the pre-commit branch, per-ext only (unbounded + runs linters — **reshapes R30·d4**); **(3)** on compaction re-inject queue+LESSONS+pointer, not full STEERING (~2.4k tok/compaction — **refines R30·d2**); **(4)** **delete `pre-compact.sh`** (a nudge-as-hook — **reverses R30·d2's PreCompact half**, restoring R28 consistency); **(5)** `refreshInterval` 1→3 (per-second wake for a mostly-static bar — **refines R30·d9**); **(6)** secret-guard generic rule → non-blocking warn, anchored keys still block (**R19**: the one sanctioned edit-breaker fires only on near-zero-FP evidence); **(7)** `tq cancel` (cancelled status — retract without a false `done` or lingering `open`); **(8)** **merge `/companion:audit` into `/companion:advise`** (**retires the standalone audit**, R30·d5); **(9)** README commands list + setup callout + glyph legend. | 2026-07-12 (owner directive, via the `advise` ritual dogfooding *itself*). The panel confirmed the enforced core is lean but that **several same-day R30 additions over-reached** — 4 of the top 5 findings were that day's own work, flagged by contexts with no stake in defending it. Recorded per **R5**; build pending, sequenced. Refines/reverses parts of **R30** (d2/d4/d5/d9), retires the audit command, and fixes a bug the suite missed; **R28 self-consistency restored** (delete d4/d2 nudge-hooks). All 9 shipped in **2.0.0** (major — the `/companion:audit` command was retired). **Demoted to 🔓 as a build-tracker (2026-07-17):** same shape as R30 — a shipped changelog frozen at 🔒. Durable survivors were promoted to real entries (**R46** the IFS fix d1; **R43/R50** the secret-guard warn-vs-block d6; d8 audit-merge done); and **d2 is now void** — it prescribes behavior for `touch.sh`, which **R51 deleted**. Treat R46/R43/R50/R51 as authoritative; this bundle is history, non-binding. |

| **R33** | 🔒 | **Under autopilot, visual/design/direction choices are parked, not auto-decided.** A taste / design / direction / wording choice is the owner's call **even when trivially reversible** — the autopilot loop parks it (`❓ [parked] <choice + options + recommendation>`) and surfaces it on return, instead of auto-resolving it under the "decide routine reversible calls yourself" rule. This holds especially when the choice arises from something the owner *asked for while autopilot is on* (that's theirs to answer). Lives in STEERING's autopilot clause + the `ask-guard` deny message. | 2026-07-12 (owner requirement, prompted by autopilot auto-picking a status-line icon it should have parked). **Refines R26**: reversibility was the only auto-decide axis; *ownership of taste* was the missing one. STEERING + a hook-message tweak, not new enforced logic — "which kind of decision is this?" is judgment (R28). |

| **R34** | 🔒 | **Ship-mode: autopilot auto-commits to a work branch; the status line leads with the plugin-state cluster.** **(a)** A per-repo `ship` flag (`/companion:autopilot ship on\|off`) — while autopilot is on, the Stop hook commits each turn's accumulated work as a reversible checkpoint on an `autopilot/*` branch (auto-created off the default to protect it; **never the default, never a push**), for the owner to review + `/companion:ship-it` on return. Best-effort; falls back to a companion git identity if the repo has none. Status shows 📦 when armed. **(b)** *(status-line ordering — a separate, cosmetic concern, split out + downgraded 🔓 2026-07-17: the signature cluster `📋/❓/⏳ · ✈️ · 📦` leads, after the beacon+shield glance, before generic model/tokens/git. Bundled here only because it shipped the same day — it has nothing to do with ship-mode.)* | 2026-07-12 (owner directive). The **safe subset** of "auto-ship under autopilot": the owner rejected auto-push/merge (it breaks the reversible-only safety model — R14 consent-on-the-consequential, R11 gating), so autopilot only **commits** (reversible); the push/merge stays the owner's manual `ship-it`. Composes with **R35**. **Known behavior:** commits to the *current* branch if it isn't the default (assumes you enabled ship-mode on a work branch); one commit per turn-with-changes, not per `tq done` (captures WIP too, squashed on ship); and it **skips** the checkpoint if the staged diff carries a high-confidence credential shape (`git add` isn't seen by the secret gate, so ship-mode re-checks — `lib` `companion_secret_re`). |
| **R35** | 🔒 | **`/companion:ship-it` merges to the default branch and prunes merged branches.** After a *successful* merge it deletes the shipped branch (local `git branch -d` — **refuses unmerged**; remote `push --delete` if it exists) and prunes other branches already merged into the default (`git branch --merged`, `-d` not `-D`, never the default/current), plus `git fetch --prune` for stale tracking refs. **Guardrail:** on a **shared** repo it *lists* merged remote branches and confirms rather than mass-deleting — a teammate's merged branch may still be wanted. | 2026-07-12 (owner directive). Extends ship-it's integrate step; the aggressive-prune option was chosen with the footgun flagged, so it's **safe-by-construction** (`-d` not force, default protected, shared-repo confirm). Composes with **R34**'s `autopilot/*` branches — the natural "autopilot accumulates → owner ships + cleans up" loop. **Note 2026-07-17:** the git-flag granularity (`-d` vs `-D`, `--prune`) is implementation and lives in `commands/ship-it.md`; the durable *decision* here is aggressive-prune, default-protected, shared-repo-confirm. |

| **R36** | 🔒 | **Autopilot is "keep-going" mode, not "owner-away".** It means *keep draining the queue without stopping* — the owner may be **present**, queuing up more tasks and keeping it on deliberately; the point is momentum, not absence. Behaviors are **unchanged** (don't stop to ask; park `❓` decisions + design/taste choices per **R33**; `⏳` owner-actions; reversible work only, binding/irreversible parked; a playtest → `⏳`) — but the framing and language drop "the owner is away / when they return." The owner reviews parked items whenever they check in; the pile is presented recommendation-first when autopilot is turned off. | 2026-07-12 (owner directive). Reshapes the framing of **R26 / R31 / R33**: it removes the present-vs-absent premise that *created* the R33 tension — there is no presence to detect if autopilot never assumes absence. The reversibility line (**R14**) still gates binding actions; that is about reversibility, not presence. Also fixed the R34 gap the owner asked for: ship-mode now **skips** a checkpoint whose staged diff carries a high-confidence key shape (`lib` `companion_secret_re`; `secret-guard` keeps its own inline copy so the gate never gains a fail-open dependency). |
| **R37** | 🔓 | **Domain-glossary layer — a Claude-facing `GLOSSARY.md` of coined terms, consulted on demand.** A project the companion governs keeps a terse `docs/GLOSSARY.md` mapping a **coined domain term → its meaning** (e.g. *"materialization cascade" for "when a lesson inside a section is made real"*). Claude **coins** a term when a concept recurs and its plain description is long, and **consults/reuses** it when naming code and writing docs — so one word carries twenty and naming stays consistent. It is **steering**, not a hook (R28: coining/consulting is judgment) and **on-demand**, not injected at SessionStart — it joins the map + ledger + stack-notes as part of a *self-describing project* (STEERING "How we know the project"), loaded when domain/naming work needs it, keeping session-start injection flat. Gotchas stay in `LESSONS.md` (R30·d7), decisions in this ledger, work in the queue — the glossary is *vocabulary only*. | 2026-07-13 (owner directive; the one transferable idea mined from mattpocock/skills' `CONTEXT.md`, the rest rejected as re-fragmenting R24 or contradicting run-in-auto/`tq`/generic). **Serves R3** (a shared vocabulary is a direct token-efficiency lever — its whole point), **generic per R9** (the model coins the terms; nothing is hardcoded), **steering-not-hook per R28**, and extends the **R24/R16** self-describing-project set beside `LESSONS.md`. Adds a convention; **reshapes/retires nothing**. On-demand (not a new always-injected doc) was the deliberate choice so it can't erode R3. **Downgraded 🔓 2026-07-17:** by its own words it "**reshapes/retires nothing**" — a purely additive, unenforced, on-demand convention has no constraint to defend, so 🔒 was the wrong tier. Token-harmless (on-demand), which is why it's fine to keep at all. |
| **R38** | 🔒 | **Turning autopilot off starts a parked-pile review — `/companion:review`, a command not a hook.** When autopilot goes off, walk the `❓ [parked]` + `⏳ [blocked]` pile **one item at a time** as recommendation-first `AskUserQuestion`s (parked items carry their options + recommendation; blocked items become *done / still-blocked / drop*), gather the owner's picks, and write each back to `tq` **before** starting new work — no-op cleanly when the pile is empty. Scope is **parked + blocked only**, *not* every `📋 open` task (open tasks need doing, not deciding). Each item can be **deferred** (kept parked) and the owner can bail — "review before new work" is the default, not a hard wall (a large pile you can't escape trains avoiding the off-switch). Triggered two ways so neither path misses it: the `/companion:autopilot off` command invokes it, **and** a STEERING autopilot clause fires it on the conversational "turn off autopilot" path. | 2026-07-13 (owner directive). **Refines R26/R33/R36** — those already say "present the parked pile recommendation-first on autopilot-off," but only as aspirational prose; this makes it a structured, reliable ritual. **Command, not a hook, per R28** — presenting recommendations is `AskUserQuestion` workflow (judgment); a hook could only inject a nudge, the R27 anti-pattern this project already retired. **Reuses the R29 `/companion:advise` loop** (options → recommendation-first ask, one at a time → close into `tq`) rather than building a parallel machine (subtract-as-you-add). The defer/bail escape and the parked+blocked-only scope were deliberate — a hard wall over the whole backlog was the rejected over-reach. **Renamed 2026-07-18 (owner directive, sign-off):** the command is now **`/companion:resume`** (was `/companion:review`) — the same ritual, renamed because R39's session-pickup is its first move, so one name (`resume`) covers pickup + parked-pile review. **This amends R38's "`/companion:review`, a command" clause** — surfaced as a visible rename (per R2/R5), not silent; the *lock* governs the ritual (autopilot-off → recommendation-first parked+blocked walk → write-back-before-new-work) and its two triggers, not the command's label. All behavior + the `manual resume`/command-gate checks preserved. **Re-split 2026-07-19 (owner directive, taken over the recommendation's advice):** the parked-pile review is again its own command **`/companion:review`** — the 2026-07-18 rename-to-`resume` is undone, so pickup (`/companion:resume`) and triage (`/companion:review`) are two separate commands (8→9 surface). This re-amends the label clause; the *lock* (the ritual + its autopilot-off trigger) is unchanged — only the command name that carries it. Surfaced as a visible trade-off (R5): the recommendation was to keep the merge (less surface); the owner chose the clearer pickup-vs-triage seam. The autopilot-off trigger, `autopilot.sh` off message, `autopilot.md`, STEERING, and the ask-guard park-guidance all now name `/companion:review`; command-gate checks updated to the split. |
| **R39** | 🔒 | **Resume is a triage handoff — it turns autopilot off, re-surfaces decisions *as parked*, then runs the review.** `/companion:resume` (script + command) exists so the owner can pull earlier-session tasks back and *decide* them, not have them autopiloted. Three coupled behaviors: **(A)** `resume.sh` clears the autopilot flag **first** — announced in one line when it was on (never a silent clobber of a persisted intent; re-arm is a manual `/companion:autopilot on`), quiet no-op when already off. **(B)** a STEERING clause documents the resume→off→park→review loop. **(D)** when reinstating carried-over tasks the model **preserves each item's classification** (`❓ [parked]` decision / `⏳ [blocked]` owner-action / `📋 open` doable) and **never promotes a parked decision into a plain open task** — the triage-vs-drain distinction lives in the task's *type*, so it survives compaction and holds in *either* mode. With autopilot then off, resume runs the **R38 parked-pile review** over the re-surfaced pile. | 2026-07-13 (owner directive: "if I run resume, disable autopilot first so I don't autopilot the answers"). **The insight:** resume doesn't *answer* anything (it re-fills the queue); the autopiloting happens on the *next drain* when a resurfaced `❓` hits the ask-guard. Fixing it by task *type* (D) rather than only a mode flag is why a resurfaced decision is safe whether autopilot is on (stays parked, R33/ask-guard) or off (R38 review walks it). **Composes with R38** (reuses its review; the resurfaced `❓`/`⏳` are exactly its scope, so R38 needs no widening to cover open tasks) and **R36** (autopilot is an owner-held intent — hence A announces the flip rather than clobbering silently). **Rejected (C):** a scoped ask-guard exemption letting resume ask *under* autopilot was folded out as **subsumed** — since A always triages with autopilot already off, a second enforced carve-out would only re-add the R27/R28 anti-pattern (a hook where a document + the existing off-switch suffice). **Amended 2026-07-17 (command folded into `review`, owner sign-off):** the standalone `/companion:resume` **command** is retired; its on-demand pickup is now **step 1 of `/companion:review`** (which runs `resume.sh`, then walks the pile). **This reverses the "script + command" clause above** — surfaced as an explicit trade-off (per R2/R5), not silently. **What's preserved (the load-bearing part):** all three behaviors — **(A)** `resume.sh` still clears autopilot first, announced; **(B)** the STEERING clause; **(D)** class-preserving reinstatement — plus every check (`manual resume: turns autopilot OFF first`, the `.root` scope tests). **What changed (the disposable part):** one fewer command in the `/` menu; the trigger is now `/companion:review`. **Why the owner accepted the reversal:** `resume` was only ever `review` + a re-surface preamble (it ran the R38 review itself); one review entry point is less surface for the same behavior. R39's *lock* now governs the three behaviors, not the command's existence. **Re-split 2026-07-19 (owner directive):** `/companion:resume` is restored as a **standalone session-pickup command**, and the parked-pile review moves back out to **`/companion:review`** — reversing the 2026-07-17 fold-into-`review`. Resume is now **pickup-only**: **(A)** `resume.sh` clears autopilot first (unchanged), **(D)** class-preserving reinstatement (unchanged), then it **hands off to `/companion:review`** for the R38 walk instead of running the review inline. All three load-bearing behaviors + every check (`manual resume: turns autopilot OFF first`, the `.root` scope tests) are preserved; what changed is the seam — pickup and triage are two moves again. Recorded as a visible reversal (R5), taken over the recommendation to keep them merged. |
| **R40** | 🔓 | **`/companion:ship-it` produces review-optimized output — clean commits, curated history, structured PR bodies — so shipped work reads as easy-to-review even when no human reviews it.** Four parts, all **steering** in `commands/ship-it.md` (R28 — judgment, no new hook): **(A)** every commit and PR body uses a **review-optimized message** — imperative subject + a body naming *What changed / Why / requirement-IDs touched / tasks closed / test result* — so even a single squashed commit is self-documenting. **(B)** an `autopilot/*` branch of per-turn `autopilot: checkpoint` commits is **curated into one commit per logical unit** (typically per `tq` task) before merge via `git reset --soft "$(git merge-base HEAD <default>)"` + re-commit by group (`rebase -i` is unavailable in the environment) — **not** merged raw and **not** flattened to one opaque squash; fallback to a single well-messaged commit when changes are too entangled to split. **(C)** the PR path gets a **structured body** (summary · changes by area · requirement-IDs · test plan+result · how to verify). **(D-lite)** a one-line **right-size nudge** — flag a large/mixed diff and offer to split it — *not* an automated stacked-PR machine (rejected as over-engineering for the typical solo/small repo). | 2026-07-13 (owner directive: "even if humans don't review the PRs, make it clean and easy to review"). **Reshapes R34's "squashed on ship"** — the ship-side of ship-mode now *curates* logical commits instead of flattening (the per-turn checkpoint capture on the Stop hook is unchanged; only how they're integrated changes), because a squashed blob is the *hardest* thing to review and defeats the goal. **Generic (R9):** the convention delegates to the project's own (Conventional Commits, ticket prefix, the version+R-ID form this repo uses) rather than hardcoding one. **Steering not hook (R28):** message-writing and history-shaping are judgment. **Composes with R35** (curate → merge → prune). D-lite was deliberately kept a nudge, not a machine — the honest bit (flag it) without the build. **Downgraded 🔓 + re-weighed 2026-07-17:** command output-format is a feature spec (steering in `commands/ship-it.md`), not a durable constraint needing sign-off to change — 🔓. And **part (B)** (curate `autopilot/*` checkpoints into logical commits via `git reset --soft`) is flagged **over-built** for "review output nobody reads": keep **(A)** (self-documenting messages — ~90% of the value) as the durable ask; make **(B)** best-effort (fall back freely to a single well-messaged commit). |
| **R41** | 🔓 | **`/companion:document` — ground the ledger from an existing repo, tiered for the agent that consumes it.** A scan→triage→record command that extracts the *load-bearing, undocumented* decisions a repo depends on and records each at the highest tier it can reach: **executable check (Claude can't ignore) › 🔒 entry (constraint) › 🔓 entry (challengeable) › dropped (incidental — recording it only adds contradiction surface + tokens).** Two honesty rules keep it from being a fabrication machine: **strength-of-why sets the lock** (a real, articulated why → 🔒 *advise must not silently reverse*; weak/unknown → 🔓 *advise may challenge*), and every entry carries a **provenance tag** (`stated`/`inferred`/`unknown`) — a labeled `unknown → 🔓` is *preferred* over a confident-but-wrong 🔒, since confident-and-wrong is the worst input to an agent. **The rationale is the owner's to fill in:** Pass-2 presents Claude's candidate whys **as the multiple-choice options** (best-guess marked `(Recommended)`, plus an **always-present open-ended "write my own rationale"** option / no-clear-why→🔓 / drop / defer), and **only the owner's active pick records an entry** — an unchosen assumption never becomes a 🔒 (the anti-laundering rule; a picked assumption is tagged `inferred`, a supplied reason `stated`). A claimed 🔒 is **challenged once** before acceptance, so legacy decisions get thought through, not hand-waved. **Two-pass** (silent ranked candidate report → interrogate only the survivors the owner keeps) keeps the ledger dense (**R3**). It is the **producer** side of the advise relationship: advise (**R29**) *consumes* `REQUIREMENTS.md`; `document` *populates* it, so advise stops guessing and stops proposing to remove a critical choice that was never written down. | 2026-07-13 (owner directive: "review an existing repo and optimize its documentation for Claude, so advise doesn't guess or override an undocumented design decision"). **Reuses the R29/R38 recommendation-first loop** (no parallel machine — subtract-as-you-add) and **R38's** defer/bail escape (no forced march over a big pile). **Extends the R24/R16 self-describing-project set** — it writes the *existing* ledger + `LESSONS.md`/`GLOSSARY.md`, inventing **no new doc format** (R24 anti-fragmentation). **Generic per R9** (scanners delegate recognition to the model; structure detected generically). **A command, not a hook, per R28** — scanning/eliciting/recording is judgment — though it may *emit* checks, which are enforcement (the tier that serves Claude best: prevention > detection). **Serves R3** — the tiering + two-pass + drop-incidental all optimize the ground truth advise reads for density and non-contradiction, the stated goal. The `document` name was the owner's pick over `excavate`/`ground`. **Downgraded 🔓 + split 2026-07-17:** the `/companion:document` *command mechanics* (scan/triage/two-pass/tiers) are a feature spec, not a constraint needing sign-off — 🔓. The durable, generalizable **honesty principles** it embodies (strength-of-why sets the lock · provenance tag · anti-laundering) are extracted to **R53** (🔒), so they govern *every* ledger write, not just this command's. |
| **R42** | 🔓 | **Parked/blocked (`❓`/`⏳`) is a prefix-view over `pending` tasks, never a `status` value.** State lives *only* as a leading emoji on a `pending` task's subject; ~5 readers re-derive it (`tq`, `stop-autopilot`, `statusline`, and `companion_open_tasks` implicitly). Keep it a prefix — do **not** promote parked/blocked to first-class `status` enum values, and do not change the glyphs in one reader without the others. | 2026-07-13 — surfaced by `/companion:document` (R41), provenance `inferred` (owner-confirmed). **Why:** one `pending`/`in_progress` query then serves *both* the active drain and cross-session resume (**R24** subtract-as-you-add), and it keeps the `status` set simple; the reader duplication is the price. Promoting to a `status` value would make `companion_open_tasks` (selects `pending`/`in_progress`) stop resurfacing parked items on resume and break **R39**'s "classification survives compaction." A bats check pins reader behavior. **Downgraded 🔓 2026-07-17:** this is the *implementation* of **R39**'s "classification survives on the prefix," not an independent decision, and the bats check is the real guard. Merged conceptually into R39; kept 🔓 as the implementation note. |
| **R43** | 🔒 | **The secret gate covers every content-writing tool — no bypass.** A credential written through *any* content-writing tool must hit `secret-guard`. *(The specific wiring — `Write\|Edit\|NotebookEdit`, reading `.content // .new_string // .new_source` — lives as a code comment; the durable requirement is the no-bypass principle, since tool names change — 2026-07-17.)* | 2026-07-13 — surfaced by `/companion:document` (R41). **Why:** the gate is the one hard, irreversible guarantee (a committed key can't be un-leaked); a tool it doesn't match is a silent hole (ship-mode's staged re-scan is only a backstop, and only when armed). Closed the `NotebookEdit` (`.new_source`) gap the scan found. **Mild R9 tension** accepted: matching a tool *name* is invocation, not language recognition. |
| **R44** | 🔓 | **`tq` writes are atomic (temp file + `mv`), never in-place.** Every task mutation goes to `.$id.tmp` then renames over `$id.json`. Do **not** "simplify" to `jq … > "$f"`. | 2026-07-13 — surfaced by `/companion:document` (R41), provenance `inferred` (owner-confirmed). **Why:** atomic rename means a task file is never half-written if jq errors or the process dies mid-write — which is what protects crash-resume (breadcrumbs + `done_when` surviving a crash, **R30·d1**); jq can't edit in place, so temp+rename is the crash-safe form. Now carries a code comment so it doesn't read as removable idiom. **Downgraded 🔓 2026-07-17 (now guarded by a check):** per R41's own tiering (check › 🔒 › 🔓), a *testable* invariant should be a **check**, not 🔒 prose. Added a bats regression guard that the temp-file+`mv` idiom stays in `tq` (catches a "simplify to `jq > $f`" refactor); with that + the code comment the 🔒 prose is redundant. Kept 🔓 as the rationale anchor. |
| **R45** | ⚰️ | ~~**`stop-autopilot.sh`'s second default-branch check (after `checkout -b`) is deliberate defense-in-depth, not dead code.**~~ The re-read + re-check of the current branch before ship-mode commits must stay. | 2026-07-13 — surfaced by `/companion:document` (R41), provenance `inferred` (owner-confirmed). **Why:** it is the last floor on **R34**'s absolute "never commit to the default branch." If any edge case (a failed `checkout -b`, detached HEAD, empty `rev-parse`) left HEAD on the default, ship-mode would `git commit` straight onto `main`. Cheap insurance on a near-irreversible invariant. The "never commit to default" outcome is exercised from both entry paths (HEAD-on-`main`, pre-existing; and detached-HEAD → the checkpoint lands off-default with `main` untouched). The **second** guard specifically only fires in a state `checkout -b` can't reproduce (its own failure), so it isn't cleanly unit-testable — it's protected by its inline `# NEVER commit to default` comment + this entry. Don't delete it as "dead code." **Retired from the ledger 2026-07-17:** a "don't refactor this line" note about one defensive line is a code-comment/LESSONS concern, not a durable *decision* (a 🔒 per defensive line is unbounded growth). The line **stays** (its comment protects it); the durable requirement is **R34**, still exercised by the R45 bats test. |
| **R46** | ⚰️ | ~~**Tab-delimited `read`s use `IFS=$'\t'` — repo-wide.**~~ Any `read` splitting a tab-joined jq line whose last field is free text (a subject) must set `IFS=$'\t'`. | 2026-07-13 — surfaced by `/companion:document` (R41). **Why:** the trailing subject can carry spaces; a default-IFS split corrupts it — the confirmed **R32·1** space-in-value bug the status line already hit. `stop-autopilot.sh`'s counts-parse silently relied on numeric leading fields; hardened to match the `statusline.sh` convention. **No dedicated test:** with the subject as the trailing `read` field the default-IFS split still works today, so a test would pass with *or* without the fix (non-discriminating). Protected by the `IFS` change + the inline code comment + this entry; the existing `stop-autopilot` tests cover regressions. **Retired from the ledger 2026-07-17 → moved to `LESSONS.md`:** a repo-wide coding-convention "watch this trap" is LESSONS content, not a decision, and it was triple-homed (code + comment + ledger). The `IFS` change + comments stay; one `LESSONS.md` line now carries the gotcha. |

| **R47** | 🔓 | **`tq report` is Design-D compact, and it never renders `done_when`.** The report is: a glyph-count header (`📋 ▸n ◻n ❓n ⏳n ✔n`), **one line per active task** (in-progress/open/parked/blocked, leading `❓`/`⏳` stripped since the glyph moves to the front), **completed shown as a count only** (no `✔ #id` lines), and a trailing **`→ next: #id`** pointer = the in-progress task, else the head of the open queue (empty when neither exists). `done_when` is **stored** on the task and **re-surfaced on cross-session resume** (`lib/companion.sh`) but is **not** printed in the live report. | 2026-07-14 (owner directive, picked from a 7-layout menu — "D", then "more icons" reverted). **Why:** the report reprints on every `add`/`doing`/`done`, so its height is paid constantly; the per-task `done_when` sub-line doubled it — trimming it keeps **crash-resume intact** (`session-start.sh` on `source:compact` still renders it via the resume renderer, **R30·d1** unbroken). **Accepted trade-off (surfaced by the ship-it devil's-advocate):** `done_when` is now *also* invisible **mid-session**, including at the moment `tq done` fires — so the acceptance test can't be re-read against at completion time; there is deliberately **no** `tq show`/re-display path (that would re-add the height D removed). If that bites in practice, the fix is a *separate on-demand* `tq show <id>`, not putting `done_when` back in the always-printed report. The `→ next` pointer is a **mechanical** queue-order default (STEERING says override it aloud when blast-radius/dependency judgment disagrees — not a hook, **R28**). **Reverses R30·d1's *render* half only** (the `done_when` **field** and its acceptance-test role are unchanged; only its appearance in `tq report` is removed). Generic per **R9** (pure formatting). **Downgraded 🔓 2026-07-17:** a CLI report *layout* is the most reversible, taste-driven surface here (and by **R33** a wording/design choice is the owner's to revisit freely); it already churned once. The recorded trade-off (done_when invisible at done-time) stays as rationale; 🔒 over-constrained a display detail. |
| **R48** | 🔒 | **The test suite is the executable half of the enforced core — kept as a regression gate, not grown feature-by-feature.** Audited 2026-07-14: the ~30 active cases (`plugins/companion/tests/*.bats` + the `check.sh` validators) are **kept** — they guard *irreversible/uneyeball-able* behavior (secret gate, the hook-fuzz "never break the user's edit" contract, autopilot control-flow + no-spin cap, ship-mode-never-touches-default, cross-repo scope) plus the `check.sh` validators (JSON/version/shellcheck/gitleaks/size). Policy going forward: **(a)** keep `check.sh` green; when an *intentional* behavior change reddens an assertion, **loosen it to behavioral** (assert state/effect, not exact output strings) rather than pinning cosmetics; **(b)** do **not** add new format-detail tests per feature — capture acceptance in the task's `--done` and verify by exercising (STEERING); a dedicated test-authoring project may backfill later. | 2026-07-14 (owner directive: "don't auto-write tests anymore, follow TDD patterns, defer a test project" — reconciled after an independent audit showed the suite is mostly enforcement, not theater, so a blanket rip-out was rejected). **Why:** the suite's brittleness was confined to exact-string assertions (Design-D **R47** reddened 3 of them: the report header/counts and the `done_when` render); those were loosened to behavioral, not deleted — deleting the spine to escape the cosmetic tail is backwards. **Reshapes the CLAUDE.md "verify everything with `./check.sh`" constraint** into keep-green-don't-grow. Also **removed 4,500 lines of stale `.bats`** under the retired-4-plugin `hud-gates` worktree (not run by `check.sh`; dead weight). **Composes with R28** — enforcement stays code, and these tests *are* the "must execute" guarantees, not advisory prose. |
| **R49** | 🔒 | **The recommendation contract: a decision-shaped request is owed options, never a flat answer.** When the request asks Claude to choose, redesign, compare, evaluate, or "what do you recommend," the response owed is **recommendation-first — 2–4 genuinely different options, each naming its cost / what it changes, the recommended one first and marked, plus the brutal-honest read** (including "don't do this"). This fires **identically whether autopilot is off or on**: off, ask it live (`AskUserQuestion`); on, the ask-guard blocks asking, so the same full payload is **parked in the `❓` subject** (`❓ [parked] <choice> — options: A)…(cost) B)…(cost); rec:<pick>+why`) for the parked-pile review (`/companion:review`) to read back via the non-truncating `tq list`. A thin one-opinion answer to a decision-shaped ask, or a parked `❓` holding a one-line guess, is a **bug** — it makes the review a rubber-stamp, the exact failure parking exists to prevent. | 2026-07-14 (owner directive, after two linked complaints: autopilot auto-answering questions that warranted input, and decision-shaped prompts not reliably getting the brutal-honest multiple-choice). **Why:** both complaints share one root — the recommendation posture was advisory STEERING prose the model skips, so it produced a flat answer live *and* a thin park-line under autopilot. **Refines R36** (keep-going ≠ decide-taste-for-the-owner), **R38/R39** (the parked pile is only a real review if each `❓` carries its options). **Generic per R9:** decision-shapedness is judged by *reading* the request, not by a keyword list — so this stays STEERING (judgment) + the ask-guard deny message (the enforced park path), never a keyword-matching hook. Not hard-enforceable (a missing `AskUserQuestion` call is nothing to block); the lever is the sharpened contract, escalating to a per-turn injection only if it keeps slipping (rejected now for its per-prompt token cost). **Deduped 2026-07-17:** R49 **operationalizes R5** (the canonical contract home) and carries **no independent authority** — its one *distinct* durable clause is the **park-the-full-payload-under-autopilot** rule; the rest cites R5. |
| **R50** | 🔒 | **One unified per-repo feature surface for the enforced core — flags gate execution, not steering clauses.** Every enforced-core capability is toggleable per repo from **one place** (`/companion:features`, `bin/features.sh`): **secret** (the gate), **steering** (the SessionStart working-agreement injection — the one output with real token cost), plus **autopilot**/**ship** *reflected + delegated* to their existing flags (no second source of truth). *(The `format` toggle was removed with `touch.sh` — **R51**.)* State lives in one per-repo file (`state_dir/features/<enc-root>`) storing only **OFF overrides** — absence = the capability's default (secret/steering default **on**). Resolution order is **env var → per-repo flag → default**: the legacy `CLAUDE_COMPANION_SECSCAN` env var stays as a **global/CI** override that wins, the per-repo flag is the normal surface. The self-contained hook (`secret-guard.sh`) reads the file with an **inline grep, no `lib` source** (fail-safe: only an explicit `secret=off` disables; any read error leaves the gate active) — preserving the "the enforced gate never fails open via a dependency" invariant; the lib-sourcing readers use `companion_feature_off` (the `set`/`state` writers were removed with the CLI 2026-07-18 — see the amendment below). Disabling **secret** warns loudly (irreversible-harm guard). | 2026-07-16 (owner directive: "each functionality a toggle the user can activate/deactivate like a feature flag"). **Scoped by the R24/R28 split, which answers "flag it?" oppositely for the two halves:** the **enforced core** (executes/blocks — the model can't ignore it) is exactly where a per-capability flag adds real control, so it gets one; **steering** is *ignorable-by-nature already* (R28), so flag-per-clause would be redundant machinery that re-fragments the one file R24 unified — **explicitly rejected** (the "full feature-flag matrix" option), leaving one steering knob: inject on/off. **Extends the R26/R34 flag-file pattern** to the rest and **unifies three prior mechanisms** (env vars, flag files, wire-or-not) behind one view — **token-neutral at runtime** (hooks *read* a file, never inject one; per R3 only injections cost tokens), the `steering` off-switch being the sole flag that *reduces* token cost. **Composes with R49** (the recommendation menu that chose this scoped design over both the do-nothing floor and the matrix). **Kept 🔒 2026-07-17 (deliberate deviation from the R40/R41 spec-downgrade):** the *feature-surface mechanics* (file format, resolution order) are revisable implementation — but R50's core is a **fail-safe safety invariant** ("the enforced secret gate never fails open via a dependency"; `secret-guard` reads the flag inline, only an explicit `secret=off` disables). Downgrading an irreversible-harm invariant to 🔓 because it shares an entry with spec detail would be the very mistake the audit warns against — so R50 **stays locked**, with the mechanics noted as the softer part. **Amended 2026-07-18 (the `/companion:features` CLI removed, owner sign-off):** the *unified toggle command* is retired — per-repo `secret`/`steering` are now set by the flag file directly (by hand or a re-add) or, for `secret`, the global `CLAUDE_COMPANION_SECSCAN=0`; `autopilot`/`ship` keep their own commands. **This reverses R50's "one command surface to view/flip capabilities"** — surfaced as a visible trade-off (R2/R5), not silent. **The LOCK is preserved where it matters (the invariant, not the UI):** the flag *mechanism* and every enforced-core **reader** are unchanged — secret-guard still honors `secret=off` per-repo (fail-safe G1, self-contained G2, isolated), session-start still honors `steering=off`, and both checks stay green (rewritten to drive the flag directly). Removed: the `features.sh` writer + `companion_feature_set/_state` (dead once the CLI was gone) + the 4 CLI-behavior tests. **Why the owner accepted it:** the toggle UI wasn't earning its surface; removing the *writer* while keeping the *gate's read* makes the secret gate marginally harder to disable (no one-command off-switch) — safe-direction. Losing the per-repo `steering` off-switch UI is the real cost (steering-off now needs a hand-written flag). |
| **R51** | 🔒 | **Move away from advisory hooks; lean into recommendation-driven, pick-from-CLI interaction.** Three decided parts. **(1) Retire the last *execute* hook** — `touch.sh` is deleted (**retires R25**): formatting is a reversible, low-stakes convenience, so it's a *steering nudge* now, not an enforced pass. The enforced core is left with **no *execute* member** — only *block* (`secret-guard.sh`), *inject* (`session-start.sh`), and *control-flow* (autopilot). This **sharpens R28**: "hooks only for execute/block/control-flow" becomes "block/inject/control-flow," since the sole execute case didn't earn enforcement. **(2) Testing** — reaffirms **R48** (owner reconfirmed 2026-07-17): TDD stays as a *design discipline* (the `tq --done "<acceptance>"` convention already encodes it), **no test-writing mandate**, and the enforcement test *spine is kept* (secret-gate / never-break-the-edit fuzz / autopilot / ship / scope). A blanket rip-out was **again rejected** — deleting the automated proof the secret gate still blocks is the one thing the brutal-honest read named the hill to die on. Only `touch.sh`'s own tests were removed (its feature is gone). **(3) Interaction shape** — the product should make **recommendation-first, pick-from-CLI options the default shape of almost every interaction** (decisions, parked-pile review, any request for owner input), with escape hatches to supply your own answer or just chat, under a **brutal-honest-at-all-times** evaluation posture. **Platform constraint (verified via claude-code-guide):** a plugin *cannot* inject "suggested prompts" / autocomplete chips — that surface is not plugin-addressable. The only plugin-addressable surfaces are **SessionStart `additionalContext` injection**, the **statusLine**, and Claude's own **`AskUserQuestion`** menus. So (3) is delivered as **STEERING prose** (the nudge engine *is* the SessionStart injection), not a new mechanism — extending **R49**'s recommendation contract from "decision-shaped asks" to "the default interaction shape," plus context-triggered nudges (debt → offer a `tq` task; large blast radius → offer to split; repetitive drain → offer autopilot). | 2026-07-17 (owner directive: "remove hooks & the test-writing mandate, keep TDD as discipline; make almost all interactions pick-from-CLI recommendations; be brutally honest at all times"). **Why:** the owner's goal — proactive, context-aware nudges the owner picks from — is produced *by* injected steering, which requires the one benign injection hook; "remove all hooks" would delete the very engine that delivers the nudges (established before scoping). So the honest reconciliation keeps the injection + block + control-flow hooks, retires only the advisory *execute* hook, and pours the effort into the steering layer. **Composes with R24** (one steering file, not scattered hooks), **R28** (the execute/block line, now sharpened), **R48** (test policy), **R49** (recommendation contract, now the default shape). The exact STEERING wording (posture intensity + nudge catalogue) was owner-selected from a recommendation menu. **Annotated 2026-07-17 (ledger sweep):** **(part 1) accepted risk** — retiring the format hook trades a deterministic, every-edit pass for **model recall**; a file can now land unformatted if the model forgets. Accepted because formatting is low-signal-cost + reversible, and `/companion:advise`'s whole-project sweep is the fallback that can reverse this on signal. **(part 2)** the R48 reaffirmation is a pointer, not new mass — see R48. **(part 3)** operationalizes **R5** (canonical contract home); the genuinely new durable fact here is the **platform constraint** (plugins can't inject prompt chips). |
| **R52** | 🔒 | **The product is the `propose → queue → drain` loop; the one-plugin *requirement* is eliminated; every prior decision is now open to challenge.** Three parts. **(1) Redesign direction (nudges + queue).** Chosen from a 4-way recommendation menu: **direction A** — reframe the whole product as one loop (context → a **recommendation-first nudge** → the owner picks/edits/declines → it enters `tq` → it drains by hand / autopilot / ship-it). **Nudges are the funnel *into* the queue, not a co-equal driver: the queue (`tq`) is the durable spine (tested, crash-safe R44), nudges are unverifiable model *content* flowing into it.** Modularity (opt-in capabilities) is delivered by **R50 runtime toggles + documenting the loop**, **not** by physically splitting into packs — **rejected option C** (a pack-split re-draws the very seams R24 collapsed; the sprawl came from the seams, not the line count) and **rejected option B** (a scored `nudges.sh` proposer — a Stop-hook injector is the R28/R30·d2 nudge-as-hook anti-pattern, and it trades model judgment for heuristics you must tune). The enforced core stays tiny: **block** (secret) · **inject** (session-start) · **control-flow** (autopilot). **(2) One-plugin packaging is no longer a hard requirement** (**amends R24**): its anti-sprawl *principle* stays locked; its single-plugin *packaging* becomes a current-state fact, so a deliberate `tq`-standalone extraction is un-blocked if it ever becomes a real goal. **(3) Standing posture — challenge everything (owner directive).** No entry is exempt: the recommendation loop should **actively surface challenges to *locked* entries**, not treat 🔒 as settled. 🔒 still means "changing it needs sign-off," but the owner has issued a **standing invitation to challenge**, so silence-by-deference is now itself a miss (composes with **R5** cite-the-R-IDs and the STEERING challenge clause). | 2026-07-17 (owner directives across one session: "move away from hooks / drive through nudges + the task-queue"; then "ground-up redesign proposal"; then, on the C-vs-reconcile fork, "brutally challenge all previous decisions; for this one go with your recommendation if we eliminated the one-plugin foundation requirement"). **Why:** the honest read is that nudges and the queue are *asymmetric* — infrastructure vs behavior — so "drive primarily through nudges" is only safe when the queue remains the spine and the nudge layer stays steering (no new enforced surface, per **R28/R51**). Elevating nudges to a mechanism (option B/C) risks re-inflating the advisory sprawl **R24** killed. Direction A also happens to be **~90% already shipped this session** (R51: options-as-default-shape, always-on honest verdict, the nudge catalogue). **Composes with R24** (principle kept, packaging freed), **R49/R51** (recommendation contract is now the default interaction shape), **R50** (toggles are the modularity mechanism). Follow-ups queued: document the loop in ROADMAP/MAP; surface a pending-nudge signal in the statusline. **Note 2026-07-17:** R52·1's "recommendation-first nudge" framing operationalizes **R5** (canonical contract home) — cite, not restate. The ledger-wide challenge pass this directive licensed **ran** (4 independent auditors): the 2026-07-17 sweep reworded/retired/downgraded R1, R3, R5–R7, R10–R21, R24, R28, R30, R32, R34, R35, R37, R40–R47 and extracted **R53**. |
| **R53** | 🔒 | **Ledger-honesty rules (extracted from R41): strength-of-why sets the lock · provenance sense · anti-laundering.** For **any** ledger entry (recorded by `/companion:document` or by hand): a real, articulated *why* → 🔒 (advise/challenge may not silently reverse it); a weak/unknown *why* → 🔓 (fair to challenge). Each entry reflects a provenance sense (`stated` / `inferred` / `unknown`) — a labelled `unknown → 🔓` is **preferred** over a confident-but-wrong 🔒, since confident-and-wrong is the worst input to an agent. **Anti-laundering:** only the owner's *active pick* records an entry; an unchosen assumption never becomes a 🔒. | 2026-07-17 — extracted from **R41** (the ledger audit found R41 bundled durable *principles* with a command *spec*). These are the load-bearing, generalizable honesty rules; the `/companion:document` mechanics that apply them are the spec (R41, now 🔓). **Composes with R5** (name what a recommendation changes) and **R52·3** (a 🔒 must survive challenge, so its *why* must be real). |
| **R54** | 🔓 | **Direction: contract-preserving regenerative `advise` — build the invariant tier FIRST.** The north star (owner-agreed) is that `/companion:advise`'s objective becomes *preserve the contract, treat everything else as disposable and fair to redesign from the ground up.* **The contract has THREE pillars, not two:** **(a) the UX record** (what the user sees/experiences), **(b) agreed NFRs / quality attributes** (only those the owner intentionally reviewed and agreed — R53 anti-laundering), and **(c) safety/correctness invariants as executable *checks*** — the things the user never sees but that must hold (secret gate fail-safe, `tq` atomic writes, autopilot-never-commits-default, cross-repo scope isolation). Pillar (c) is the one the naive "UX + NFRs only, ignore the rest" framing omits — and a ground-up regen that omits it *silently deletes* those invariants (a leaked key, a corrupted store). So "ignore everything else" is rejected in favour of "**unconstrained *except by the contract***": regen must reproduce the UX, meet the NFRs, and pass every invariant check. **Sequencing (owner-agreed, non-negotiable): checks first, THEN regen-by-default** — the invariant-check tier must exist and be proven before `advise` may default to regeneration; regen is a **confirmed, bounded, check-gated** operation, never a silent auto-rewrite. | 2026-07-17 (owner directive). **Why:** the principle is a clean generalization of **R53** (only intentionally-agreed → durable; everything else is disposable) and the R41 tiering (check › 🔒 › 🔓). But it only works if the *invisible* invariants are captured as checks — which is why this session's audit deliberately kept R44/R45/R50's fail-safes locked. **The safe-sequencing was reached after pushback:** the owner's first framing (UX+NFRs only, ignore the rest, regen by default *now*) was challenged as a footgun; the owner agreed to build the contract+invariant tier first. **Composes with R41** (`document` does the contract triage + tagging {UX-contract | agreed-NFR | safety-invariant | incidental}), **R48** (the test suite IS the invariant net), **R29** (advise), **R53** (anti-laundering). **Build-pending** (kept 🔓 per the R30/R32 lesson — no 🔒 build-bundle); the concrete pieces (UX record, invariant checks, advise reframe) earn their own entries when built. |
| **R55** | 🔓 | **`/companion:advise redesign` — whole-app contract-preserving redesign; the *logged* contract is UX + quality attributes only.** Refines **R54**. **(1) Log two things, not three.** What the owner *documents* is **UX** (`docs/UX.md`) + **quality attributes** (`docs/NFR.md`). The **safety invariants aren't a prose contract the owner maintains** — the *enforcement* is **executable checks** (`check.sh`); `docs/INVARIANTS.md` is a pointer/legend to those checks (a small maintained table), not the source of truth. A check runs on its own; you don't hand-maintain it as contract. **Cataloguing *technical requirements* is dropped** (owner directive) — implementation is disposable. This costs no safety *because the net was never the catalogue* — R54 always put invariants in the **check** tier, not prose. **(2) New `redesign` mode of `advise`:** document UX+QAs → then a **whole-application** ground-up redesign executed as a **sequence of bounded, check-gated per-module passes** (reuses `advise regen <target>`, R54/#17, as the per-pass engine) — **never a single unbounded rewrite**. Each pass: gate on green checks → regenerate the module against UX+QAs → apply on-branch → re-check; a reddened check stops that pass. | 2026-07-17 (owner directive: "throw away cataloguing technical requirements; log only UX + quality attributes; a command like advise that triggers a ground-up redesign once those are documented"). **Reached after pushback:** the owner confirmed the redesign stays **gated on the existing invariant checks** (not UX+QAs alone — dropping the gate would silently delete the fail-safe secret read / atomic writes / never-commit-main) and runs in **bounded passes** (not one whole-app shot). **Composes with R54** (contract + checks-first + regen engine), **R41** (`document` populates UX/NFR), **R29** (advise shape), **R48** (the checks are the net). **Amended 2026-07-17 (command split + DA hardening):** promoted from `advise` sub-modes to **standalone commands** `/companion:regen` + `/companion:redesign` (discoverability — arg-modes never showed in the `/` menu; owner-picked), leaving `advise` critique-only. Devil's-advocate-hardened before ship: an explicit **autopilot-clear step-0** on both edit flows (a code-editing gate can't rest on advisory prose), a **`redesign` D0 invariant-coverage gate** (verify the net covers the app *before* the first whole-app pass — R54 sequencing), **auto-revert-on-red** (not "offer to"), and honest framing of the **G3/G4 residual** (a regen can drop the uncheckable R45 guard with `check.sh` still green — the owner ack is the only guard there). Build-pending. **Amended 2026-07-18 (regen folded into redesign, owner sign-off — reverses the command-split amendment above):** the standalone **`/companion:regen` command is retired**; its per-target rebuild engine (R1–R5) is now **inlined as redesign's D3 per-module pass**, and `/companion:redesign` **requires `/companion:document` as its D1 first step** (log UX+NFR before any module is touched). **Why the owner accepted reversing the split:** a bounded single-target rebuild only ever ran as one redesign pass, and R56 flags whole-app redesign as the *less* trusted operation — keeping two commands for one engine wasn't earning its surface. **What's preserved:** the R1–R5 engine verbatim (bound · checks-first-or-refuse · regenerate-against-contract · confirm+apply-on-branch+auto-revert-on-red), now guarded by the R56 command-gate test against `redesign.md` (was `regen.md`). **What changed:** one fewer command; a single-target rebuild is now a scoped redesign pass. `document` stays standalone (still feeds `advise`, R41). |
| **R56** | 🔓 | **Complete the behavior net before trusting whole-app `redesign` — but the command prose has a hard ceiling.** `redesign`'s D0 gate correctly **stopped** a blind whole-app rebuild of this mature app: the net pins the *safety* invariants but not every *observable behavior* — proven by the statusline regen silently dropping the autopilot-beacon (a green rebuild that regressed the UX). Decision: **characterize the executable core** (`bin/`+`lib` — write behavior-pinning tests for the gaps, like the beacon test) **and structurally-guard the command prose** (each command prompt must retain its critical gate steps — a textual check like R44/G2). **The command layer is fundamentally NOT blind-regenerable** — a prompt's behavior is *Claude's judgment*, unverifiable by a check (R28: prose is ignorable-by-nature); the ceiling is structural guards + always bounded-regen-with-human-verify (R5). So whole-app `redesign` becomes safe for the **core**, never for the **commands**. | 2026-07-17 (owner directive, after trying `/companion:redesign` — D0 stopped it, owner chose "complete the net first"). Essentially the deferred **R48** test-authoring project, scoped by this ceiling. **Composes with R54/R55** (the net *is* the gate), **R48** (regression-gate — this backfills it), **R28** (prose = judgment = unverifiable). Build-pending, phased: **(1)** behavior-coverage audit of the core → gap list; **(2)** characterization tests for the gaps; **(3)** structural guards for the command prompts. |
| **R57** | 🔓 | **`/companion:ship-it` is contract-aware — it keeps the recorded contract from drifting a commit behind the code.** Three coupled behaviors, all in the ship-it *command prose* (judgment, not a hook — R28/R51): **(a) name the contract impact** — read the diff, identify which R54 pillar it touches (UX / NFR / invariant), and fold the *relevant logged design* (the affected `docs/UX.md` rows / `NFR.md` entry / invariant) into the commit + PR body, **loudest for UX** so a reviewer sees the experience delta; **(b) propose the UX-doc update recommendation-first** — when a ship alters UX, ship-it drafts the `docs/UX.md` edit (rows to add/change/remove, preserving every `[E]/[S]` kind + check), the owner confirms/adjusts (parked as `❓` under autopilot), and on approval the doc edit is **staged with the code so they land in one commit** — never a silent rewrite; **(c) maintain a README docs index** — a `Documentation` section linking each `docs/*.md`, refreshed when a doc is added/removed/renamed, so a GitHub reviewer reaches the contract in one click. Generated contract docs default to a single `docs/` folder (R41/`document`). | 2026-07-18 (owner directive: "ship-it should include content relevant to the changes / design, especially UX; auto-update the UX docs; docs in a docs folder + README linking them"). **Why:** the UX contract (R54 pillar a) is only trustworthy if it stays current — but auto-*rewriting* it during the ship would flip it from *source-of-truth-the-code-obeys* to *doc-that-chases-code*, gutting its power as an independent check. So the design keeps the contract **owner-governed** (propose → confirm → commit-together) with the **drift-guard bats check as the backstop** (a mismatch still fails CI). **Rejected alternatives (owner-picked the middle):** silent auto-rewrite (fast, but contract chases code and the drift check becomes vacuous) and hard-block-on-drift-don't-fix (strongest, most friction). **Composes with R40** (review-optimized ship output — this extends it to the contract), **R54** (the pillars it syncs), **R38/R49** (the recommendation-first proposal shape), **R41** (`document` owns the `docs/` routing). Lives in `ship-it.md` prose (R28 ceiling per R56 — structurally guarded by the command-gate test, behavior is Claude's judgment). |
| **R58** | 🔒 | **The living contract — the UX + quality-attribute record stays accurate at any moment while developing, as ONE plugin, not a second system.** The owner asked for "a completely accurate UX / non-functional-requirements / quality-attributes document at any moment." Delivered as a new **pillar inside `companion`** (not a split — see Why), across the R28 block/inject/steer line: **(a) capture** — a `UserPromptSubmit` hook (`bin/capture.sh`) banks every prompt to a per-repo write-only store (`companion_captures_dir`), **injecting nothing** (N1 — zero runtime token cost); raw material, no judgment; **(b) the contract reflex** — STEERING: when a request/edit changes what the user sees/does (UX) or a quality attribute (NFR), **move the contract doc first** (recommendation-first) and queue the code against it — the continuous twin of `/companion:document`'s batch sweep, the doc-side of TDD; **(c) the drift backstop** — `bin/contract-drift.sh` (generic/R9, run by `check.sh` + `ship-it`) surfaces behaviour changed without a contract doc moving — **advisory (warns, exit 0), not a hard gate**, because "does this touch the contract" is a judgment a gate can't make without false-positiving into disablement; **(d) `/companion:cover`** — the test-recommendation arm: ranks UX paths by criticality × coverage gap and recommends the ideal test per critical path in the repo's own idiom, queued — **recommends, never writes** test files. | 2026-07-19 (owner directive, provenance **stated**: "split into two systems … keep the UX/NFR/quality doc completely accurate at any moment … capture the prompts that represent risk automatically … recommend the ideal tests … be brutally honest"). **Why one plugin, not two (the brutal-honest call the owner accepted):** a separate planning plugin would duplicate the queue/secret-gate/statusline and add a **second SessionStart injection** — the one output with real token cost — directly against **N1**, and reverse **R24**/**R52** (the whole history is *collapsing* systems). The contract is not separable from execution: it's what `ship-it` syncs (R57), `advise` critiques, `redesign` rebuilds against (R54). The split only pays off for a **non-coding audience** installing planning docs on a repo they don't build; absent that user, it's YAGNI. **Capture is a hook** (durable I/O the model can't self-do) but **write-only/no-injection** so it doesn't breach N1; **classification stays steering** (judgment, R28); **drift is a check** but **advisory** (prevention is the reflex, detection is the net — N7's gate would misfire here). **Composes with:** R54 (the pillars it keeps live), R57 (`ship-it`'s at-ship sync — R58 makes it continuous + adds the backstop), R41/`document` (the batch producer R58's reflex is the JIT twin of), R48/R51 (`cover` inherits "a test only where it's a durable net"), N1/N2/N3/R28. |

**Not in the ledger, by design:** AGENTS.md (maintainer-guide prose) and owner **memory** (personal, cross-session, lives outside the repo).
