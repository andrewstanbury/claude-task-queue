{
  "name": "charter",
  "version": "0.6.0",
  "description": "Know the project: gates substantive work on documented quality attributes (web projects get Lighthouse-aligned defaults — CWV, a11y, print CSS, progressive enhancement, components-by-default), maintains a committed Claude-facing roadmap/backlog file (cross-session/engineer coordination), keeps a project map (file→responsibility index) so a session orients from the map instead of re-scanning the tree, and tracks decisions/ADRs so past choices aren't re-litigated. Generates the docs from the codebase/git history when missing, reconciles them when present, and goes quiet once the policy is summarised in CLAUDE.md (claude-companion marker). Read-only over the project (the hook detects; the model authors the files).",
  "author": { "name": "Andrew Stanbury" },
  "homepage": "https://github.com/andrewstanbury/claude-task-queue/tree/main/plugins/charter",
  "repository": "https://github.com/andrewstanbury/claude-task-queue",
  "license": "MIT",
  "keywords": ["quality-attributes", "nfr", "architecture", "hooks", "claude-md", "orientation", "roadmap", "backlog", "project-map", "web", "accessibility", "performance", "lighthouse", "adr", "decisions"],
  "defaultEnabled": true
}
